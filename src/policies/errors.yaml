version: 1
errors:
  MISSING_CHANGE_DIR:
    message: A change is required.
    fix: Use --change <change-name>.
  AMBIGUOUS_CHANGE_DIR:
    message: Multiple changes match the provided value.
    fix: Pass one of the listed candidates as --change.
  CHANGE_DIR_NOT_FOUND:
    message: No matching change was found.
    fix: Use one of the changes listed in data.available_changes, or create a new change with --request.
  ARTIFACT_NOT_FOUND:
    message: Required artifact not found.
    fix: Run the corresponding stage first.
  ARTIFACT_PARSE_FAILED:
    message: Artifact YAML could not be parsed.
    fix: Fix the YAML syntax.
  CANNOT_ACCEPT:
    message: The artifact cannot be accepted yet.
    fix: Resolve blocking findings and complete semantic validation.
  REVIEW_NOT_PASSING:
    message: Review is not passing.
    fix: Resolve blocking findings and complete semantic validation.
  CONFLICTING_DECISION:
    message: Conflicting review decision flags.
    fix: Use either --accept or --reject, not both.
  UNKNOWN_COMMAND:
    message: Unknown workflow or command.
    fix: Use --list-workflows to see available workflows.
  UNKNOWN_STAGE:
    message: Unknown stage.
    fix: Use a known stage name.
  UNKNOWN_STEP:
    message: Unknown step.
    fix: Use a known step name.
  INTERNAL_ERROR:
    message: Internal error.
    fix: Inspect the error details.
  PLAN_NOT_FOUND:
    message: plan.yaml not found.
    fix: Run the planning stage first.
  TASK_NOT_FOUND:
    message: Task not found in plan.yaml.
    fix: Use a valid TASK-NNN id.
  INVALID_TASK_STATUS:
    message: Invalid task status.
    fix: Use pending, in_progress, done, blocked, or skipped.
  MISSING_TASK_UPDATE_FIELDS:
    message: Task update requires both --task-id and --status.
    fix: Provide both --task-id and --status.
  TASK_DONE_REQUIRES_NOTE:
    message: Marking a task done requires a non-empty implementation note.
    fix: Re-run the update with --note describing what was implemented.
  CANNOT_COMPLETE:
    message: Knowledge extraction cannot complete.
    fix: Resolve validation errors and mark entries extracted.
  IMPLEMENTATION_NOT_ACCEPTED:
    message: Implementation review is not accepted.
    fix: Complete implementation review before completing knowledge extraction.
  ILLEGAL_STATUS_TRANSITION:
    message: Illegal lifecycle status transition.
    fix: Follow the lifecycle defined in src/policies/lifecycle.yaml.
  SEMANTIC_NOT_COMPLETE:
    message: Semantic validation is not complete.
    fix: Record semantic validation results with sufficient evidence.
  STRICT_FINDING:
    message: Strict mode converted an advisory finding into a blocking error.
    fix: Resolve the advisory finding or rerun without --strict.
  POLICY_INVALID:
    message: A policy file is invalid.
    fix: Fix the policy file and rerun validation.
  STAGE_POLICY_MISSING:
    message: The stage policy file is missing.
    fix: Restore requirements-policy.yaml in the requirements stage folder.
  STAGE_POLICY_INVALID:
    message: The stage policy file is invalid.
    fix: Fix the named field in requirements-policy.yaml and rerun.
  UNKNOWN_LENS:
    message: The lens is not in the policy vocabulary.
    fix: Use one of the lenses listed in requirements-policy.yaml.
  DOCS_INDEX_MISSING:
    message: docs/current/index.md not found.
    fix: Run the knowledge-init skill to create docs/current/index.md.
  SCHEMAS_MISSING:
    message: No schemas directory found.
    fix: Deploy or restore the runtime schemas.
  POLICIES_MISSING:
    message: No policies directory found.
    fix: Deploy or restore the runtime policies.

  USAGE:
    message: Invalid command usage.
    fix: Check required flags and try again.
  UNKNOWN_STAGE_FILTER:
    message: Unknown stage filter.
    fix: Use --stage requirements, design, or planning.
  MISSING_EXTRACTION_NOTE:
    message: Marking a docs-delta entry extracted requires a note.
    fix: Provide --note with at least 10 characters.
  MISSING_MARK_TARGET:
    message: Marking extracted requires an entry id or target doc.
    fix: Provide --entry-id or --target-doc.
  ENTRY_ID_NOT_FOUND:
    message: No docs-delta entry matched the provided entry id.
    fix: Use a valid DD-... entry id from docs-delta.yaml.
  TARGET_DOC_NOT_FOUND:
    message: No docs-delta entries matched the provided target doc.
    fix: Use a target_doc listed in docs-delta.yaml.
  UNPLANNED_FILE:
    message: A task changed a file not listed in the plan.
    fix: Update the plan or explain the incidental change.
  PREVIOUS_STAGE_NOT_READY:
    message: A previous stage artifact is not ready or accepted.
    fix: Complete the previous stage before finalizing this stage.
  REQUIREMENTS_NOT_READY:
    message: Requirements are not ready or accepted.
    fix: Complete requirements before finalizing downstream stages.
  ARTIFACT_INITIALIZED:
    message: A new artifact was initialized.
    fix: Continue the workflow and fill in the artifact.
  DOCS_DELTA_VALIDATION:
    message: Docs-delta validation found problems.
    fix: Fix docs-delta entries before completing knowledge extraction.
  NODE_VERSION_UNSUPPORTED:
    message: Node.js version is not supported.
    fix: Use Node.js 20 or newer.
  MANIFEST_INVALID:
    message: Deployed runtime manifest is invalid.
    fix: Redeploy the runtime with bin/deploy-to-agent.mjs.
  STAGE_MISSING_DESCRIPTOR:
    message: A stage folder is missing its stage.yaml descriptor.
    fix: Create stage.yaml in the stage folder.
  STAGE_INVALID_DESCRIPTOR:
    message: A stage.yaml descriptor is invalid.
    fix: Fix stage.yaml to match the stage meta-schema.
  STAGE_UNKNOWN_KIND:
    message: A stage descriptor declares an unknown kind.
    fix: Use authoring, review, tasks, or aggregator.
  STAGE_ID_MISMATCH:
    message: A stage folder name does not match its descriptor id.
    fix: Rename the folder or fix the descriptor id.
  STAGE_CYCLE:
    message: The requires graph contains a cycle.
    fix: Remove the cycle from the requires fields.
  STAGE_MISSING_REFERENCE:
    message: A stage requires a stage that does not exist.
    fix: Create the stage folder or fix the requires field.
  CHECK_UNKNOWN:
    message: A structural-checks.yaml entry names an unknown check.
    fix: Use a check from the capped catalog.
  CHECK_INVALID_PARAMS:
    message: A structural-checks.yaml entry carries malformed parameters.
    fix: Fix the check parameters for the named check.
  STAGE_GATE_BLOCKED:
    message: A stage cannot run because a required stage is not accepted.
    fix: Complete and accept the required stage before running this stage.

  AGENT_SCHEMA_INVALID:
    message: An agent definition file fails the agent meta-schema.
    fix: Fix the agent file to match agent.schema.yaml and keep its id equal to the filename stem.
  AGENT_PROMPT_MARKER:
    message: An agent system prompt contains CLI or skill instruction markers.
    fix: Remove the marker from the system prompt so the personality prose stays neutral.
  AGENT_REF_UNRESOLVED:
    message: A stage.yaml agent reference does not resolve.
    fix: Reference an agent id defined in src/agents/ or remove the stage binding.
  AGENT_PERMISSION_INCOMPATIBLE:
    message: A stage-to-agent binding violates the permission compatibility check.
    fix: Align the stage's permission contract with the agent's declared permissions.
  PLATFORM_UNKNOWN:
    message: Deployment platform or version selection does not resolve.
    fix: Use a known platform and version for the deployment target.
