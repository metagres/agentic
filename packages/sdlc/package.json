{
  "name": "@agentic/sdlc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic SDLC toolkit CLI (canonical source, P-01 scaffold).",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "sdlc-cli": "./dist/bin/sdlc-cli.js"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsup"
  },
  "dependencies": {
    "@agentic/checks": "*",
    "citty": "0.2.2",
    "yaml": "^2.9.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2"
  }
}
