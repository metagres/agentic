{
  "name": "agentic-sdlc-toolkit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Development repository for the agentic SDLC toolkit used by OpenCode-compatible AI coding agents.",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "sdlc": "src/scripts/sdlc.js"
  },
  "scripts": {
    "test": "node --test test/unit/*.test.js test/e2e/*.test.js test/contracts/*.test.js",
    "deploy": "node bin/deploy-to-agent.js",
    "sdlc": "node src/scripts/sdlc.js",
    "build": "node bin/build-runtime.js",
    "validate:contracts": "node bin/validate-contracts.js",
    "validate:templates": "node bin/validate-templates.js",
    "lint:artifact": "node bin/lint-artifact.js",
    "validate": "npm run validate:schemas && npm run validate:policies && npm run validate:contracts && npm run validate:templates && npm test",
    "test:unit": "node --test test/unit/*.test.js",
    "test:e2e": "node --test test/e2e/*.test.js",
    "test:contracts": "node --test test/contracts/*.test.js",
    "validate:schemas": "node bin/validate-schemas.js",
    "validate:policies": "node bin/validate-policies.js",
    "validate:fixtures": "npm run test:contracts",
    "check:all": "npm run test:all && npm run deploy:smoke",
    "deploy:smoke": "node bin/deploy-to-agent.js --dest .tmp/agent --project-root .tmp/project --bundle --clean",
    "test:all": "npm run validate && npm run test:unit && npm run test:contracts && npm run test:e2e"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "ignore": "^7.0.6",
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
