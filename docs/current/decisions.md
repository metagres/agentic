# decisions.md

## Living: Cycle Decisions

| Date | Context | Decision | Rationale | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| 2026-08-23 | genericize-stage-engine (DEC-001) | Each stage is one folder under src/stages/<stage-id>/ holding all of its configuration; the deploy script copies this directory like the other asset directories. | DL-001 asks whether stage configuration stays centralized or moves to per-stage folders; the repository already bundles src/templates, src/schemas, and src/policies. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-002) | Structural checks live in their own structural-checks.yaml file per stage, parallel to semantic-checks.yaml; stage.yaml carries identity, kind, artifact, and requires only. | The requirements originally placed named checks inside stage.yaml; design review split them out for symmetry with semantic-checks.yaml and to keep the descriptor focused on identity and topology. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-003) | Stages may not ship validation scripts; all structural validation is declarative through named checks in structural-checks.yaml. Logic no existing check expresses becomes a new catalog entry via design review. | The original requirements allowed optional colocated validator scripts; design review removed them because the capped catalog covers every current check and a second extension path would recreate hidden coupling. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-004) | The catalog is exactly the eleven checks enumerated in CMP-004, each with a fixed parameter shape; adding or changing a check is a design-review event, the single extension path for validation logic. | The requirements flag the risk that a generic check library accumulates ad-hoc parameters and recreates hidden coupling. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-005) | Review stages are folders with kind review, a -review id suffix, and a reviews reference to the reviewed stage; they carry stage.yaml and steps.yaml, participate in the requires graph, and are invoked as stage commands. The review --target command is removed. | DL-003 models review gates as normal stages; the dedicated review command with --target duplicates stage knowledge. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-006) | The kinds are authoring, review, tasks, and aggregator, each with one interpreter; an unknown kind in a descriptor is a startup error naming the folder and the value. | FR-004 requires a fixed kind set with interpreters, so new stages of existing kinds need no code. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-007) | Pipeline order is a topological sort of the requires graph with an alphabetical stage-id tie-break for deterministic output; no explicit order or sequence field exists. | DL-008 and DL-012 require ordering purely from the requires graph as a DAG. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-008) | A requires edge is satisfied only while the required stage's tracked artifact has status accepted; review stages are runnable when their tracked artifact is ready-for-review or accepted. Stages declare requires on all predecessors whose current acceptance they depend on (planning requires requirements-review and design-review). | A review having run does not imply its findings were resolved; DL-014 defines gate satisfaction by artifact acceptance. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-009) | A single validateArtifact function per stage serves authoring --finalize, review stages, and bin/lint-artifact.ts; no stage-specific validation logic exists in the review path. | Today the review path re-implements the composition that internal finalize already uses, risking divergent findings (DL-006). | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-010) | The based_on_requirements and based_on_design checks are removed; the metadata fields remain as provenance recorded at init from template substitution, and predecessor readiness is enforced by the acceptance gate instead. | DL-007 drops version equality checks between artifacts of the same change. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-011) | pipeline.yaml and ids.yaml are deleted; id patterns stay enforced by the per-stage schemas and next-ids mappings live in stage.yaml. errors.yaml remains central as cross-cutting policy, and requirements-policy.yaml moves into the requirements stage folder. | DL-010 replaces pipeline.yaml and ids.yaml with directory discovery and the requires graph. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-012) | The CLI refuses to start on a missing stage.yaml, invalid YAML, an unknown kind, an id mismatch, a requires cycle, or a missing requires reference, naming the offending folder, cycle, or stage; a structural-checks entry naming an unknown check or malformed parameters aborts validation naming the stage folder and entry. | DL-011 requires hard errors for malformed stage folders. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-013) | No migration path is provided for artifacts created by the current layout; the deployed skill is versioned per build and consumers upgrade deliberately. AGENTS.md is rewritten to describe the stage-folder layout and its invariants. | DL-013 and DL-004 accept a breaking change and freedom to redesign, including the AGENTS.md rules file. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-014) | The envelope keeps exactly the seven top-level fields; the dispatcher becomes registry-driven but normalization and the envelope schema are unchanged. | NFR-003 freezes the CLI response shape. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-015) | The deploy script copies src/stages/ into the bundle and compiles hooks modules to JavaScript; the bundle contains no package.json, no node_modules, no source TypeScript, and exactly one SKILL.md whose workflow list is rendered from the registry. | NFR-002 requires the deployed skill to stay one self-contained folder bundling every stage. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-23 | genericize-stage-engine (DEC-016) | steps.yaml drives the step machine through declarative complete_when predicates; a stage may ship an optional hooks module for extra steps and commands, which is the only stage-specific code allowed and never participates in validation. | Step sequences and completion predicates are hardcoded per stage today, and the requirements stage has behavior (discovery gate, record-answer, set-clarity) that no declarative predicate expresses. | accepted | docs/changes/genericize-stage-engine/design.yaml |
| 2026-08-24 | deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole (DEC-001) | Deploy knowledge-init as its own self-contained skill folder beside agentic-sdlc, source-controlled at src/skills/knowledge-init/. | knowledge-init lived only in the gitignored .opencode runtime, and bundling it into agentic-sdlc would load a run-once heavyweight procedure into every lifecycle session. | accepted | docs/changes/deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole/design.yaml |
| 2026-08-24 | deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole (DEC-002) | Remove the docs-init workflow entirely; the CLI never creates docs/current; knowledge-init is the only creator. | docs-init seeded a skeleton that conflicted with knowledge-init's richer baseline and left an unfilled overview stub. | accepted | docs/changes/deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole/design.yaml |
| 2026-08-24 | deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole (DEC-003) | Index columns are exactly File, Purpose, When to Read, Notes; the row-number, token-size, and last-updated columns are dropped. | The six-column index carried bookkeeping columns, while the index's job is telling the agent what to read and when. | accepted | docs/changes/deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole/design.yaml |
| 2026-08-24 | deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole (DEC-004) | index.md is the single entry document; no overview.md is generated or seeded. | overview.md was written only as an unfilled stub by docs-init, read by nothing, and absent from knowledge-init outputs. | accepted | docs/changes/deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole/design.yaml |
| 2026-08-24 | deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole (DEC-005) | Delete src/templates and its consumers; validate-templates.ts keeps its path and gains skill frontmatter validation. | src/templates contained only the docs-index template, and renaming bins is out of scope for this change. | accepted | docs/changes/deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole/design.yaml |
| 2026-08-24 | deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole (DEC-006) | The aggregator emits DOCS_INDEX_MISSING as a warning with a message naming the knowledge-init skill; no new error code is added. | A new error code would require catalog and unit test updates, and doctor already owns this code for the same condition. | accepted | docs/changes/deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole/design.yaml |
| 2026-08-24 | deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole (DEC-007) | The knowledge-init manifest.json carries name, version, and deployedAt only. | knowledge-init ships no scripts, and its manifest exists for version traceability. | accepted | docs/changes/deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole/design.yaml |
| 2026-08-24 | deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole (DEC-008) | Its smoke verifies SKILL.md presence and frontmatter: name equals the folder name and description is non-empty. | The smoke test runs the deployed CLI, but knowledge-init has no CLI to run. | accepted | docs/changes/deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole/design.yaml |
| 2026-08-24 | deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole (DEC-009) | Fixtures list knowledge-init documents in their index fixture, and helper deltas retarget to docs/current/architecture.md. | E2E fixtures and helper deltas target overview.md, which no longer exists after this change. | accepted | docs/changes/deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole/design.yaml |
| 2026-08-24 | deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole (DEC-010) | The output keeps its existing fields and adds skills with name and smoke per skill. | The deploy JSON is not a frozen CLI envelope and must report both skills per FR-004. | accepted | docs/changes/deploy-knowledge-init-as-a-second-skill-and-make-it-the-sole/design.yaml |
| 2026-08-24 | llm-first-requirements-authoring-redesign (DEC-001) | requirements-policy.yaml is the only source of discovery thresholds; the fallback constants are deleted and every policy failure blocks the stage with a named error code. | The current discoveryGate silently falls back to hardcoded thresholds when the policy is missing or unreadable, so shipped behavior can drift from the file (problem statement, DL-003). | accepted | docs/changes/llm-first-requirements-authoring-redesign/design.yaml |
| 2026-08-24 | llm-first-requirements-authoring-redesign (DEC-002) | The authoring interpreter invokes an optional hooks startup(env) entry point right after environment construction; hooks throw Errors carrying a code property, and the existing catch already maps err.code through makeError, so no new error plumbing is needed. The mechanism is generic and available to any stage's hooks. | FR-001 and FR-002 require blocked envelopes with specific error codes for any requirements command, but hooks currently have no early entry point and thrown errors surface as INTERNAL_ERROR. | accepted | docs/changes/llm-first-requirements-authoring-redesign/design.yaml |
| 2026-08-24 | llm-first-requirements-authoring-redesign (DEC-003) | The discovery step advances only when the policy floor passes and metadata.discovery_reviewed is true, the flag being settable only through complete-step with step discovery. complete-step does not re-evaluate the gate: the step machine is the single routing authority, so a premature confirmation cannot bypass the floor. | LLM agents satisfice against count-based gates (problem statement); AC-005 requires the discovery step to remain active after count-only passage. | accepted | docs/changes/llm-first-requirements-authoring-redesign/design.yaml |
| 2026-08-24 | llm-first-requirements-authoring-redesign (DEC-004) | The scenarios step is routed from hooks.extraStep like discovery and assumptions, and its steps.yaml entry carries no complete_when predicate; the step definition supplies only title, markdown, commands, and exit criteria. | FR-006's activation rule (any open scenario, or empty and unconfirmed) needs per-element status inspection, which the declarative complete_when predicate vocabulary cannot express. | accepted | docs/changes/llm-first-requirements-authoring-redesign/design.yaml |
| 2026-08-24 | llm-first-requirements-authoring-redesign (DEC-005) | The schema's lens enum keeps design and adds data; the policy vocabulary excludes design, so record-answer rejects it with UNKNOWN_LENS while old entries remain valid and count toward resolved totals. | In-flight artifacts may carry design-lens discovery entries (NFR-002), while FR-004 defines an eight-lens vocabulary with data and AC-003 uses design as the rejected example. | accepted | docs/changes/llm-first-requirements-authoring-redesign/design.yaml |
| 2026-08-24 | llm-first-requirements-authoring-redesign (DEC-006) | Promotion is drafting-step instructions plus the scenario-coverage advisory at finalize and review; the back-filled ac_id, once present, is validated by ref-exists. Escalation to a structural catalog check remains a future design-review event, matching the accepted advisory-only EARS assumption. | FR-008 requires promotion of resolved ac-outcome scenarios, but the check catalog is capped at eleven and ref-exists skips absent fields (verified assumption), so the presence of ac_id cannot be enforced without a new check type. | accepted | docs/changes/llm-first-requirements-authoring-redesign/design.yaml |
| 2026-08-24 | llm-first-requirements-authoring-redesign (DEC-007) | category is enum-constrained to the four values for deterministic machine parsing (NFR-001), and scenario entries allow additional properties so the taxonomy can evolve without schema migration. | The requirements accept happy, edge, negative, and boundary as the taxonomy while flagging that real changes may need more (unverified assumption). | accepted | docs/changes/llm-first-requirements-authoring-redesign/design.yaml |
| 2026-08-24 | llm-first-requirements-authoring-redesign (DEC-008) | A non-empty scenario set with every entry resolved advances without confirmation; the flag exists to make an empty scenario set an explicit decision rather than an oversight. | FR-006 activates the scenarios step while any scenario is open or the set is empty and unconfirmed; AC-008 advances past all-resolved scenarios without mention of the flag. | accepted | docs/changes/llm-first-requirements-authoring-redesign/design.yaml |
| 2026-08-24 | llm-first-requirements-authoring-redesign (DEC-009) | The policy loads when the stage executes — every command that constructs the stage environment, including bare invocations and --next-ids; the three static introspection commands are out of scope because they never load stage configuration. | AC-001 says any requirements command blocks on a missing policy, but --help, --describe, and --describe-step return before the stage environment is constructed and answer from static steps.yaml content. | accepted | docs/changes/llm-first-requirements-authoring-redesign/design.yaml |
| 2026-08-24 | llm-first-requirements-authoring-redesign (DEC-010) | Policy shape validation is strict (version 1, exactly three anchors, required_lenses subsets of the vocabulary, positive integer minimums) and names the offending field path. An artifact clarity outside the anchors fails the gate with clarity_valid false and a named reason instead of defaulting; the schema enum independently flags the value as a blocking finding. | FR-002 requires field-naming errors, and the current gate silently defaults an unknown clarity to partial — another silent fallback. | accepted | docs/changes/llm-first-requirements-authoring-redesign/design.yaml |
| 2026-08-24 | llm-first-requirements-authoring-redesign (DEC-011) | --finalize continues to validate schema, structural checks, and the semantic checklist without requiring discovery or scenarios confirmation; the step machine remains the authority for the authoring flow, and review acceptance operates on structural and semantic findings. E2e fixtures therefore set the confirmation flags explicitly. | --finalize currently validates structure without consulting the step machine (the assumptions step has the same property), and the requirements scope the confirmation to step routing only. | accepted | docs/changes/llm-first-requirements-authoring-redesign/design.yaml |
| 2026-08-25 | add-agent-agnostic-agent-definitions-deployed-alongside-the- (DEC-001) | Agent definitions live in neutral YAML under src/agents/ with a neutral permission vocabulary; all platform-specific knowledge (directory, naming, frontmatter, permission keys, model qualification) lives in deployment-layer renderers. | The toolkit must stay usable with any coding agent (invariant 7), but agents are configured per platform; the user requires the source to be agnostic and deployment to specialize. | accepted | docs/changes/add-agent-agnostic-agent-definitions-deployed-alongside-the-/design.yaml |
| 2026-08-25 | add-agent-agnostic-agent-definitions-deployed-alongside-the- (DEC-002) | The agent meta-schema enumerates the 23 fully qualified opencode-go ids as a const enum; any other model value is a schema violation. Catalog updates are schema-change events. | The allowed LLMs are the opencode-go catalog; references must be unambiguous and machine-checkable. | accepted | docs/changes/add-agent-agnostic-agent-definitions-deployed-alongside-the-/design.yaml |
| 2026-08-25 | add-agent-agnostic-agent-definitions-deployed-alongside-the- (DEC-003) | stage.yaml gains an optional agent field; absent means unbound (agent null in CLI data, the current agent runs the stage). A present but unresolvable reference is a hard validation error. | The user requires that stages without an agent run in the current agent; binding must not become mandatory. | accepted | docs/changes/add-agent-agnostic-agent-definitions-deployed-alongside-the-/design.yaml |
| 2026-08-25 | add-agent-agnostic-agent-definitions-deployed-alongside-the- (DEC-004) | The binding surfaces only inside the existing data field (workflows[] entries and status stage entries gain agent); the generated SKILL.md carries one pointer sentence, not a table. | The mapping must be machine-actionable and survive post-deploy stage additions; the envelope shape is frozen. | accepted | docs/changes/add-agent-agnostic-agent-definitions-deployed-alongside-the-/design.yaml |
| 2026-08-25 | add-agent-agnostic-agent-definitions-deployed-alongside-the- (DEC-005) | No LLM judgment anywhere. The engine owns a permission contract per stage kind (the interaction profile of each kind interpreter), stage.yaml may override individual keys, and a deterministic check at validation and deploy time verifies every binding (floors must be allow, ceilings must be deny, multi-stage bindings satisfy union of floors and intersection of ceilings). Instruction changes that exceed a kind profile require an override in the same change, which validation then forces against the bound agent. | CLI instructions evolve and can outgrow hand-authored agent permissions; the user asks whether compatibility needs LLM judgment at deploy time. | accepted | docs/changes/add-agent-agnostic-agent-definitions-deployed-alongside-the-/design.yaml |
| 2026-08-25 | add-agent-agnostic-agent-definitions-deployed-alongside-the- (DEC-006) | Deployment selects a renderer by --platform and --platform-version from a deployment-layer registry; each renderer owns directory, naming, frontmatter mapping, permission translation, and version variants. Skills deploy identically for every platform; platform configuration affects only agent rendering. Defaults preserve current OpenCode behavior. | Agent formats differ across platforms and platform versions (OpenCode v1 tools vs v2 permission; other coding agents in principle), while the SKILL.md protocol is cross-framework. | accepted | docs/changes/add-agent-agnostic-agent-definitions-deployed-alongside-the-/design.yaml |
| 2026-08-25 | add-agent-agnostic-agent-definitions-deployed-alongside-the- (DEC-007) | A single stage-reviewer agent is bound by all four review stage descriptors; its contract ceiling (file_write deny) enforces the append-only invariant at the agent level. | The review stages share identical mechanics (gate check, findings, semantic checklist, accept or reject, CLI-owned append-only rounds); the roster must be minimal where definitions genuinely overlap. | accepted | docs/changes/add-agent-agnostic-agent-definitions-deployed-alongside-the-/design.yaml |
| 2026-08-25 | add-agent-agnostic-agent-definitions-deployed-alongside-the- (DEC-008) | A constant marker list (sdlc command flags, bundled CLI path, envelope field names, step command placeholders) is checked against every system_prompt at validation time; matches fail naming the file and marker. Role and personality prose is unconstrained. | System prompts must not duplicate instructions the CLI or skill instructions already return; free-form personality prose must stay allowed. | accepted | docs/changes/add-agent-agnostic-agent-definitions-deployed-alongside-the-/design.yaml |
| 2026-08-25 | add-agent-agnostic-agent-definitions-deployed-alongside-the- (DEC-009) | The agent checks live in bin/validate-policies.ts beside the requires-graph check (engine-level config validation), with error codes in src/policies/errors.yaml; the capped catalog is untouched. | Invariant 11 caps structural validation at eleven named checks operating on stage artifacts; agent validation is config cross-validation across files. | accepted | docs/changes/add-agent-agnostic-agent-definitions-deployed-alongside-the-/design.yaml |
| 2026-08-25 | agent-invocation-modes-and-interactive-questioning-agent-def (DEC-001) | An omitted mode field resolves to all; a definition may declare subagent or primary explicitly to narrow its invocation paths. | The user wants every stage agent directly invocable without editing the roster; explicit opt-down must remain possible. | accepted | docs/changes/agent-invocation-modes-and-interactive-questioning-agent-def/design.yaml |
| 2026-08-25 | agent-invocation-modes-and-interactive-questioning-agent-def (DEC-002) | requirements-analyst carries question allow; the five other roster agents carry explicit deny; kind contracts leave question unconstrained so the compatibility check imposes nothing. | Structured questions ease discovery conversations, but other roles have no need per stakeholder instruction. | accepted | docs/changes/agent-invocation-modes-and-interactive-questioning-agent-def/design.yaml |
| 2026-08-25 | agent-invocation-modes-and-interactive-questioning-agent-def (DEC-003) | The OpenCode renderer maps mode verbatim (primary, subagent, all) and question to its native question key (permission.question in v2, tools.question in legacy v1); a future platform renderer reduces values it cannot express. | Target runtimes express invocation modes and tool gating differently; the agnostic layer must stay neutral. | accepted | docs/changes/agent-invocation-modes-and-interactive-questioning-agent-def/design.yaml |
| 2026-08-25 | streamline-the-sdlc-process-economy-collapse-the-authoring-s (DEC-001) | A single acceptance_criteria list with category and parent_id; completed changes are never migrated because they are final. | Measured 99 percent average text similarity between promoted pairs; double authoring and review for identical content. | accepted | docs/changes/streamline-the-sdlc-process-economy-collapse-the-authoring-s/design.yaml |
| 2026-08-25 | streamline-the-sdlc-process-economy-collapse-the-authoring-s (DEC-002) | Transitions and updates validate their own completeness at write time; review gates stop re-discovering construction errors. | Eleven review-time findings traced to one missing-note root cause; fourteen schema errors from unnormalized delta entries. | accepted | docs/changes/streamline-the-sdlc-process-economy-collapse-the-authoring-s/design.yaml |
| 2026-08-25 | streamline-the-sdlc-process-economy-collapse-the-authoring-s (DEC-003) | Omit step_help by default; --help-step restores it; top-level envelope shape untouched. | step_help dominated envelope payloads on every call while experienced agents rarely read it. | accepted | docs/changes/streamline-the-sdlc-process-economy-collapse-the-authoring-s/design.yaml |
| 2026-08-25 | streamline-the-sdlc-process-economy-collapse-the-authoring-s (DEC-004) | Six steps with one-call finalize; record-answer and set-clarity stay functional; recovery paths keep their dedicated steps. | Eight step stops taxed capable agents without catching what mattered; gates that remained were self-confirmed. | accepted | docs/changes/streamline-the-sdlc-process-economy-collapse-the-authoring-s/design.yaml |
| 2026-08-25 | streamline-the-sdlc-process-economy-collapse-the-authoring-s (DEC-005) | validate runs the seconds-long suite; check:all keeps full coverage before completion. | Validate including e2e deploy builds was slow enough that implementers skipped it. | accepted | docs/changes/streamline-the-sdlc-process-economy-collapse-the-authoring-s/design.yaml |
| 2026-08-26 | add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode (DEC-001) | Place the audit as a SKILL.md folder skill under src/skills/agent-audit/ with no hardcoded agent paths, following the same structure as src/skills/knowledge-init/. bin/deploy-to-agent.ts remains unchanged except for rendering effectiveModel; it does not copy or bundle the audit skill into .opencode/skills/. The skill is invoked manually and ends with a reviewable git diff. | The audit's LLM judgment must not leak into runtime validation or deployment, and the SKILL.md folder protocol is the established dev-only skill convention (cf. src/skills/knowledge-init/). Deployment currently bundles exactly two skills. | accepted | docs/changes/add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode/design.yaml |
| 2026-08-26 | add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode (DEC-002) | On every audit run, fetch GET http://opencode.ai/zen/go/v1/models and rewrite the model enum in src/schemas/agent.schema.yaml to exactly the sorted opencode/<id> qualifications. The first run migrates every stored opencode-go/<id> in the schema and in src/agents/*.yaml model fields to opencode/<id>; afterwards no opencode-go/ prefix remains. | The stored enum lists 23 opencode-go/<id> values while the live endpoint returns 30 bare ids with owned_by opencode, so the catalog has already drifted and stale ids break validation. Stakeholder fixes qualification as opencode/<id>. | accepted | docs/changes/add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode/design.yaml |
| 2026-08-26 | add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode (DEC-003) | Add optional model_override as a non-empty free-form string not enum-checked; validation rejects only the empty string. The model field stays the team recommendation and remains enum-checked. The skill never overwrites an existing model_override; refreshing the catalog updates only model. | Individuals need a personal LLM preference without destroying the team suggestion, and refreshing the catalog must not overwrite personal choices. model_override must be free-form so any provider id can be used. | accepted | docs/changes/add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode/design.yaml |
| 2026-08-26 | add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode (DEC-004) | AgentRecord exposes modelOverride (or null) and effectiveModel = modelOverride ?? model via loadAgentFile/loadAgentRegistry. Deployment renders effectiveModel into frontmatter. CLI data surfaces both model and effectiveModel. No additional branching points. | Deployment must render a single model value while keeping the recommendation visible in source. Registry and surface layers need a consistent precedence rule. | accepted | docs/changes/add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode/design.yaml |
| 2026-08-26 | add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode (DEC-005) | The skill reuses computeEffectivePermissions and checkAgentCompatibility in a loop, rewriting agent permissions until the check passes for every bound stage. No new contracts, vocabularies, or LLM judgment for permissions; the capped check catalog is untouched. | Agent permissions can drift from what bound stages require; stage effective requirements are already defined deterministically as kind contract overridden by stage permissions, and checkAgentCompatibility already names offending stage, agent, and key. | accepted | docs/changes/add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode/design.yaml |
| 2026-08-26 | add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode (DEC-006) | LLM-driven decisions (web-grounded model assignment, temperature/description/system_prompt realignment, roster add/remove judgments) occur only inside the dev-time audit skill. Validation (enum membership, non-empty override, permission compatibility, purity markers) and deployment (effective-model rendering) are purely deterministic with zero LLM or network calls. | Model selection and parameter realignment require judgment about capabilities and alignment with stage purpose, but validation and deployment must stay reproducible and not call the network or an LLM. | accepted | docs/changes/add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode/design.yaml |
| 2026-08-26 | add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode (DEC-007) | Dedupe collapses identical definitions to one survivor with all stage.yaml references repointed. Add creates a new valid agent YAML and binds it; remove deletes the YAML and its bindings when no stage needs it. All operations validate against agent.schema.yaml and stage.schema.yaml so unresolved-reference errors surface immediately. | Duplicate definitions waste bindings and a stage purpose may not be well served by any existing agent; conversely redundant agents should be removed. Stage bindings in stage.yaml are the pipeline topology and must stay consistent. | accepted | docs/changes/add-a-dev-only-agent-audit-skill-that-refreshes-the-opencode/design.yaml |
| 2026-08-26 | explicit-change-slugs-at-creation-passing-change-together (DEC-001) | The LLM passes its judged slug via the existing --change flag on the creation invocation; the script validates and falls back to mechanical slugify for bare --request. No rename support. | Slug quality needs knowledge of the request essence, which the calling agent has; uniqueness and charset need none. | accepted | docs/changes/explicit-change-slugs-at-creation-passing-change-together/design.yaml |
| 2026-08-26 | streamline-the-sdlc-process-economy-collapse-the-authoring-s (CMP-002, CMP-007) | The tasks interpreter owns task state on plan.yaml (started_at, completed_at, files_changed set on transitions) and planning bookkeeping becomes optional (complexity, milestones, risks optional lists; the template stops emitting task bookkeeping fields) | Plans should carry authored content only; the CLI derives runtime state mechanically | accepted | docs/changes/streamline-the-sdlc-process-economy-collapse-the-authoring-s/design.yaml, src/scripts/lib/kinds/tasks.ts, src/stages/planning/schema.yaml |
| 2026-08-27 | agent-delegation-loop (DEC-001) | Compose in normalizeEnvelope via getStageById on the resolved workflow id, prepending CMP-001 output when a bound stage resolves. cli.ts gains an import of stage-registry (no import cycle - stage-registry depends only on yaml-io/schema/paths/types). Cross-cutting commands and unknown ids resolve to nothing and stay quiet, giving FR-002 for free. Future kinds inherit the behavior with zero code. | DL-003 demands the directive be composed from the declarative binding and never drift from it. Two seams exist - each kind interpreter call site (dozens of writeJson invocations across authoring/review/tasks/aggregator) or the one funnel every envelope passes through (writeJson -> normalizeEnvelope, 22 callers, which already reads payload.workflow/stage). | accepted | docs/changes/agent-delegation-loop/design.yaml |
| 2026-08-27 | agent-delegation-loop (DEC-002) | The directive phrases identity conditionally ("if you are already <agent-id>, proceed yourself") and the model judges its own identity from context. No current_agent data field, no env-var plumbing, no new envelope field (NFR-001, AC-009). Revisit only if a standardized identity signal appears. | All six agents are mode all, so a bound stage may be read by the bound agent itself; OpenCode exposes no dependable current-agent signal to a running agent (requirements assumption, DL-004). | accepted | docs/changes/agent-delegation-loop/design.yaml |
| 2026-08-27 | agent-delegation-loop (DEC-003) | The directive carries an explicit availability clause instructing the caller to proceed running the stage itself when the named agent is not present or not invocable. The CLI computes nothing about availability; judgment stays with the calling agent, which has the runtime view. | The toolkit process cannot probe the consuming platform's agent roster - agents exist in the runtime, not in the CLI (DL-006). | accepted | docs/changes/agent-delegation-loop/design.yaml |
| 2026-08-27 | agent-delegation-loop (DEC-004) | Review-kind directives name the bound reviewer agent and state the round must be performed by it, not by the authoring agent; SKILL.md repeats the convention. The limitation is recorded here and in the requirements failure paths; detection remains human audit of review rounds. | The CLI has no caller-identity signal, so any mechanical guard against an authoring agent running its own review would be theater (DL-005). | accepted | docs/changes/agent-delegation-loop/design.yaml |
| 2026-08-27 | agent-delegation-loop (DEC-005) | Prepend the directive as a distinct paragraph separated by a blank line ahead of existing instructions. It never replaces or rewrites step guidance, keeping every existing instruction string intact below it. | Instructions already carry step-specific guidance; the directive must be seen before the agent starts executing steps itself. | accepted | docs/changes/agent-delegation-loop/design.yaml |
| 2026-08-27 | agent-delegation-loop (DEC-006) | Strengthen workflow item 4 of SKILL_TEMPLATE in bin/deploy-to-agent.ts into the firm rule; leave skill-manifest.ts metadata-only. The smoke asserts a stable marker phrase ("is run via that agent") so wording tweaks cannot silently break AC-007 coverage. | AGENTS.md names skill-manifest.ts as the generation source, but the manifest carries only id/title/description while the deploy bin owns the rendered body (SKILL_TEMPLATE). | accepted | docs/changes/agent-delegation-loop/design.yaml |
| 2026-08-27 | agent-delegation-loop (DEC-007) | Unit tests exercise the pure composer directly and drive the funnel against fixture directories through loadStageRegistry(cwd, stagesDir), which already accepts an explicit stagesDir - no chdir gymnastics, no production test hooks. | AC-010 requires fixture stages bound to arbitrary agent ids to prove composition is binding-driven; normalizeEnvelope resolves stages from process.cwd() like the rest of the engine. | accepted | docs/changes/agent-delegation-loop/design.yaml |
| 2026-08-27 | rehome-improvement-review-canon (DEC-001) | Goals become the "## SDLC Goals" section of docs/current/capabilities.md (CMP-001, DM-001) and baselines become the "## Baselines" section of docs/current/operations.md (CMP-002, DM-002); both mutate only through changes via knowledge extraction, and every review run consumes them read-only. The improvement-review skill becomes a pure advisor whose only write surface is docs/ideas (CMP-003): findings become proposals whose Status carries disposition, goal amendments travel as proposals, and fairness lives inside proposals. The playbook dissolves after register resolution (CMP-004). This supersedes DEC-001 of sdlc-improvement-review-skill; its frozen artifacts stay untouched and remain the cited method record. Rejected alternatives — a tenth docs/current document (breaks the index contract for content that is the why behind existing documents); keeping canon in docs/ideas (queue-semantics violation, the defect being fixed); a runtime bootstrap for goals or baselines (no runtime bootstrap ever exists — the initial content is authored by this change through its own pipeline). | The frozen DEC-001 of sdlc-improvement-review-skill (2026-08-27) stored the SDLC goals list inside docs/ideas/sdlc-improvement-review-playbook.md beside the register and baselines, on the grounds that docs/current writes are reserved to knowledge extraction and the skill folder must not accumulate run state. Dogfooding showed the deeper defect: docs/ideas is a proposal queue by contract, and hosting mutable canon inside the queue blurs what is proposal and what is law, while the skill holds run-time write power over its own evaluation standards. Invariant 4 reserves docs/current writes to knowledge extraction — which is not a blocker but the correct path: canon lands through a change's delta entries, and the skill's write surface shrinks to the queue. | accepted | docs/changes/rehome-improvement-review-canon/design.yaml |
| 2026-08-27 | rehome-improvement-review-canon (DEC-002) | Implementation runs API-001 (artifact volume total, M-01), API-002 (nine per-stage envelope byte rows, M-02 through M-10, measured on this change's slug), and API-003 (validate duration, M-11) from the repo root AFTER all file work completes including the playbook deletion, so rows reflect the completed state. API-004 (transcript events, M-12) runs only if the maintainer supplies transcript files; otherwise M-12 is a pending marker (DM-003): Value "pending", kind and unit declared, recipe in the comparability note, no delta claim, replaced rather than appended by the first successful measurement. The prose baselines migrate verbatim as the authoritative qualitative record (AC-023); the frozen two-tier semantics and the ~2 s-to-ms prose anchor mapping carry over unchanged. Rejected alternatives — inventing historical numbers (prohibited by the frozen DEC-002 rationale); letting the first review run bootstrap rows (a runtime bootstrap would exist after all, and the skill must not write docs/current). | FR-008 requires initial quantitative rows produced by actually running the bundled helpers during this change's implementation, with pending markers and recipes for any metric a helper cannot measure. The helpers are read-only and byte-identical (frozen DEC-008 scoping), so running them writes nothing — the rows land via the knowledge-extraction delta like every other docs/current change. | accepted | docs/changes/rehome-improvement-review-canon/design.yaml |
| 2026-08-27 | rehome-improvement-review-canon (DEC-003) | The goals gate becomes a read-only load (CMP-003 section 2): a run that judges a goal missing or amendment-worthy writes a docs/ideas proposal stating the proposed entry in canon format; the landing change carries the capabilities.md delta (AC-010). The What-worked fairness rule moves inside proposals: a run that produces proposals must cite at least one confirmed-working mechanism with dated evidence inside a proposal (a "What worked" note in its Evidence section); a run with zero substantiated findings writes nothing and the duty is vacuous. Proposal Status adjudication (DM-004 vocabulary) replaces the register's disposition column, and the referential-integrity sweep becomes a write-time duty on each proposal's References. The thin-signal warning and confirmation are recorded in the run's proposals, its only durable output. Rejected alternatives — keeping a fairness store outside proposals (any store outside docs/ideas violates the write-surface shrink; a fairness file inside docs/ideas would itself be canon-in-queue, the defect being fixed); dropping the fairness rule entirely (complaint-only reviews lose credibility — the frozen DEC-006 rationale stands). | FR-005 replaces the skill's run-time canon-write duties with read-only consumption. The frozen design's goals gate (CMP-007) proposed-and-stored a missing goals list, and the register refresh (CMP-008) maintained the What-worked fairness subsection in place. With the register dissolved and canon read-only, both write paths need new homes that keep their value without canon writes. | accepted | docs/changes/rehome-improvement-review-canon/design.yaml |
| 2026-08-27 | rehome-improvement-review-canon (DEC-004) | F7 resolves to the existing docs/ideas/p12-fast-track-lane.md (verified present, Status Proposed). F5 closes Fixed by dated disposition: both complaints delivered most directly by the completed docs/changes/agent-delegation-loop/ change (delegation directive composer src/scripts/lib/delegation.ts; the change hosted from the original p11 proposal and the one that actually closed the loop), corroborated by add-agent-agnostic-agent-definitions-deployed-alongside-the- (stage binding, envelope surfacing, rendered agents, stage-reviewer roster with file_write deny) and agent-invocation-modes-and-interactive-questioning-agent-def (mode resolution, question gating), with the review separation-of-duties convention; fallback per failure path 5, preserve-without-duplicating — if implementation verification finds any F5 citation unresolvable or the evidence ambiguous, record the row's preservation as a dated disposition in this change's resolution record (DM-005), citing the existing hosted p11 at docs/changes/agent-delegation-loop/p11-agent-delegation-loop.md (frozen per AC-026); never create a duplicate docs/ideas/p11 file. F4 closes Fixed by dated disposition: the optionalization is provenance-verified (streamline CMP-002/CMP-007) and the residue "flag inventory still open" is absorbed by the standing Step-4 unused-surface inventory duty (SKILL.md section 7) — a per-run procedure with mandatory grep evidence, not a pending one-time item. F1, F2, F3, F6 and the two unlabeled rows close with verified citations: F1 to streamline DEC-004; F2 to streamline DEC-001 plus dedupeDeltas in src/scripts/lib/kinds/aggregator.ts; F3 to streamline DEC-002; F6 to streamline DEC-003 and DEC-005; the docs-index row to the streamline plan task plus the noise filter in src/scripts/lib/docs-index.ts; the slug row to docs/changes/explicit-change-slugs-at-creation-passing-change-together/. Implementation re-verifies every citation resolves AND states the claimed content, recording results in plan task notes before deletion; an unresolvable citation stops deletion until the row is preserved (AC-008). Rejected alternatives — creating a docs/ideas/p11 duplicate unconditionally (the file already exists hosted and frozen at docs/changes/agent-delegation-loop/p11-agent-delegation-loop.md; a duplicate would land a stale proposal whose substance is delivered, polluting the queue); deleting the playbook without resolution (loses regression-check history, violates FR-004). | FR-004 requires every register row carrying open residue to resolve before the playbook is removed, and every Fixed row's provenance to be verified present in decisions.md or completed change artifacts. Evidence gathered during design (2026-08-27): p12 exists on disk; the completed agent-delegation-loop change plus the agent-layer changes delivered F5's substance; the flag-inventory residue of F4 is absorbed by the skill's standing Step-4 duty; all other rows' citations resolve. | accepted | docs/changes/rehome-improvement-review-canon/design.yaml |
| 2026-08-27 | rehome-improvement-review-canon (DEC-005) | The Status landing signature is exactly "Landed in <change-slug> <YYYY-MM-DD>" (machine-checkable regex "^Landed in \\S+ \\d{4}-\\d{2}-\\d{2}$"), defined once in the conventions.md File Organization row and the glossary Idea Document entity so the convention and the AC-025 check are one text (DM-004). The move duty lives as an implementation-stage task pattern in the implementing change: its plan.yaml carries a documentation-type task "Land idea <slug>" that sets the Status and moves the file unmodified (apart from the Status row) into docs/changes/<change-slug>/ before the change completes; the engine does not enforce this — no catalog or envelope changes — the convention plus review do. Implementation-review verifies the landing: hosted file with signature Status, docs/ideas containing zero files whose Status matches the signature (the mechanical grep check), and a diff limited to the Status row plus the path move; an idea still in docs/ideas at review time evidences a skipped or late implementation task and blocks the round. After completion the hosted file is a frozen change artifact; later work references it read-only (AC-026). Rejected alternatives — a knowledge-extraction or aggregator-enforced move (engine change, prohibited); verification at knowledge-extraction time (too late to distinguish a skipped implementation task from an extraction-order artifact); a registry section tracking landed ideas (a second canon store duplicating what the Status field already carries). | FR-009 requires the completing change to update each implemented idea's Status with the change slug and landing date and move the file into its own change folder during its implementation stage, with AC-025's machine check and the convention shipping together, and AC-026 freezing hosted files. The requirements-review advisory pinned verification to implementation-review so review-time state distinguishes stage-timing violations. | accepted | docs/changes/rehome-improvement-review-canon/design.yaml |
| 2026-08-27 | rehome-improvement-review-canon (DEC-006) | The six live sites are retargeted as specified in CMP-006: SKILL.md (by the CMP-003 rewrite), AGENTS.md section 10 row and codemap.md row (direct edits, they are not knowledge-extraction targets), and the capabilities.md, operations.md, glossary.md rows (via the knowledge-extraction deltas). Excluded from the live set and left untouched: the historical decisions.md DEC-001 row (append-only), the frozen artifacts under docs/changes/sdlc-improvement-review-skill/, and this change's own artifacts (frozen on completion). The residual sweep is an implementation verification step — a repo-wide grep for "sdlc-improvement-review-playbook" excluding .opencode/, .tmp/, node_modules, and llm_context.txt; any hit outside the exclusion set, the history row, and the frozen artifacts is retargeted before finalization, and a remaining live reference means the change is not complete (failure path 3). Rejected alternatives — migrating the frozen change's artifacts to the new paths (violates NFR-003/AC-019); rewriting the decisions.md history row (violates append-only discipline for decision records). | FR-006 requires every live reference to the playbook path retargeted or removed, with AC-011 counting zero live references outside append-only historical records and frozen completed-change artifacts, and AC-012 excluding the frozen sdlc-improvement-review-skill artifacts from the count. A repo-wide grep on 2026-08-27 confirmed exactly the six live sites plus the excluded records. | accepted | docs/changes/rehome-improvement-review-canon/design.yaml |
| 2026-08-27 | sdlc-improvement-review-skill (DEC-001) | Store the goals list as a dedicated section of docs/ideas/sdlc-improvement-review-playbook.md beside the findings register and baselines it is consumed with. One method document already refreshed in place by this skill, git-diff reviewable, not living-doc governed (docs/ideas confirmed free-form during requirements review), and it inherits the playbook failure path of loud abort on move. Rejected alternatives - docs/current/ violates invariant 4; the skill folder conflates source with run state; a separate docs/ideas file scatters review state across two homes. | FR-010 requires proposing, storing, and loading a dated SDLC goals list but names no location. AGENTS.md invariant 4 reserves docs/current/ writes to knowledge extraction, and the skill folder is version-controlled source that must not accumulate mutable run state. | accepted | docs/changes/sdlc-improvement-review-skill/design.yaml |
| 2026-08-27 | sdlc-improvement-review-skill (DEC-002) | Split baseline semantics into tiers. Qualitative prose baselines remain authoritative and are never regenerated. Quantitative baselines are DM-003 rows created by the rule recorded-by-a-prior-run-where-present-otherwise-record-initial-baselines - on first run every measurement helper executes once and its output lands as initial-baseline rows carrying command, date, and no delta claim; later runs delta against the newest row of the same metric kind. This bootstraps both AC-027 (same units and field order for direct delta comparison) and AC-007 (measurement outputs in the same units and shapes as the recorded baselines). Unit mapping for the timing anchor - the playbook prose baseline npm run validate at about 2 s maps onto API-004's duration_ms as approximately 2000 ms, so the first initial-baseline row for metric validate-duration records kind timing, unit ms, and the prose-to-ms correspondence in its comparability note. AC-027 and AC-007 become true from the second run onward without inventing historical numbers. | AC-027 and AC-007 both assume 2026-08 baselines hold numeric measurements they do not; the playbook baselines section is qualitative prose plus a few inline figures, so direct delta comparison has no numeric foundation yet. The prose figure npm run validate at about 2 seconds is the only duration anchor and it names no unit compatible with tooling output. | accepted | docs/changes/sdlc-improvement-review-skill/design.yaml |
| 2026-08-27 | sdlc-improvement-review-skill (DEC-003) | Place the four helpers at src/skills/improvement-review/scripts/. tsconfig includes src/**/*.ts so the fast gate typechecks them. The requirements failure-path fallback (relocate to bin/ or src/scripts/) is retired - verification eliminated the risk. | UA1 assumed validation tolerates files beside SKILL.md in a dev-only skill folder; no dev-only skill bundled scripts before. Verified against source - bin/validate-templates.ts scans src/skills folders checking only SKILL.md frontmatter (name equals folder, non-empty description) with no sibling-file restriction, and bin/deploy-to-agent.ts deploys exactly two skills by explicit id, copying knowledge-init by file only and never scanning src/skills folders. | accepted | docs/changes/sdlc-improvement-review-skill/design.yaml |
| 2026-08-27 | sdlc-improvement-review-skill (DEC-004) | Helpers are ESM TypeScript using node builtins only (fs, path, child_process, perf_hooks), zero new npm dependencies, invoked as node src/skills/improvement-review/scripts/name.ts. No bundling, no compile step; determinism and dependency-freedom follow from builtins plus the existing typecheck gate. | Repo convention runs TypeScript without a build step (package.json scripts invoke node bin/*.ts; engines node >=20; installed node v25 strips types natively). | accepted | docs/changes/sdlc-improvement-review-skill/design.yaml |
| 2026-08-27 | sdlc-improvement-review-skill (DEC-005) | mine_transcript.ts accepts only explicitly supplied file paths and never discovers transcript stores. Extraction targets a generic line-oriented event grammar documented in SKILL.md - sdlc CLI invocation lines counted per stage, repeated identical consecutive command lines as wasted-round candidates, delegation events capturing the three FR-002 sub-fields (delegation type used, model resolution success/failure, whether the delegated output needed rework) - with the source filename printed beside every number. Zero parseable events produce the explicit zero-extraction report and non-zero exit. Absent transcripts degrade the review to instrumentation notes only with cycles marked reconstructed-or-missing; degradation is a designed mode, not an error path. | DL-003 wants post-hoc transcript mining; UA2 notes transcript locations vary by runtime and machine; invariant 7 forbids hardcoded agent-specific paths. | accepted | docs/changes/sdlc-improvement-review-skill/design.yaml |
| 2026-08-27 | sdlc-improvement-review-skill (DEC-006) | Maintain a What-worked subsection inside the Findings register section during every refresh, with entries of mechanism, evidence (measurement, count, or sourced observation), and date. A review with zero fairness entries is incomplete and must not finalize its register update. The disposition vocabulary gains the single canonical token Works alongside Fixed and Open; every Works row carries its dated confirmation as a mandatory confirmed-working history entry (DM-002), so positives undergo the same dated regression-check discipline as fixes without a second vocabulary name. | FR-009 requires recording what worked alongside complaints; the playbook warns that complaint-only reviews lose credibility but defines no format. | accepted | docs/changes/sdlc-improvement-review-skill/design.yaml |
| 2026-08-27 | sdlc-improvement-review-skill (DEC-007) | The DM-005 skeleton is grounded solely on the playbook Step 6 inline enumeration plus p12-fast-track-lane.md as the concrete exemplar; no P11 reference appears anywhere in the skill. Register refresh gains a referential-integrity sweep over pointers to proposal files - dangling pointers such as F5's are flagged in the round history and corrected or marked missing through the skill's own register mechanics, never through engine changes. | Advisory 4 - the playbook F5 row references p11-agent-delegation-loop.md, which does not exist on disk (docs/ideas holds only the playbook and p12-fast-track-lane.md), so P11 cannot ground the template. | accepted | docs/changes/sdlc-improvement-review-skill/design.yaml |
| 2026-08-27 | sdlc-improvement-review-skill (DEC-008) | Scope byte-identity to state-derived helpers (measure_artifacts, envelope_sizes, mine_transcript). validate_duration.ts guarantees shape stability - fixed field order, fixed units - while duration_ms varies; DM-003 rows of kind timing carry comparison semantics of orders of magnitude and regression detection, not byte equality. Recorded so review reads AC-025 as scoped, not violated. | AC-025 demands byte-identical output for identical repository state across helper runs, while FR-004 mandates a bundled helper covering validate duration, whose value depends on machine and load rather than repository state alone. Taken literally together the two are unsatisfiable for a timing metric. | accepted | docs/changes/sdlc-improvement-review-skill/design.yaml |
| 2026-08-28 | mine-transcript-delegation-stem (DEC-001) | The design interprets NFR-001 as the determinism contract. For inputs where neither the pre-fix stem nor the four-form stem matches any word, the delegation branch is inert in both versions and the only changed line is the stem constant, so stdout bytes and exit code are preserved; the AC-008 case asserts this observable class. Divergence on inputs the pre-fix stem over-matched, such as the delegateion form, is the bug fix working as specified and is pinned by a boundary test so the four-form scope cannot silently widen. | NFR-001 and AC-008 state byte-identical stdout for a transcript containing no word matching the delegation stem. Read as a universal quantifier over all such transcripts the claim is falsifiable: a line such as delegateion type=x rework=y matches the pre-fix stem but not the four-form stem, so pre-fix and post-fix outputs differ on that input by design. The miner header and the frozen scoping decision define byte-identity as determinism, byte-identical output for identical input files. | accepted | docs/changes/mine-transcript-delegation-stem/design.yaml |
| 2026-08-28 | mine-transcript-delegation-stem (DEC-002) | The suite commits exactly one fixture at test/fixtures/mine-transcript/delegation-transcript.txt, grouped under a directory named for the miner, mirroring the existing directory-per-concern layout of the fixtures tree. Every other acceptance case writes its small transcript into a temp directory created with fs.mkdtempSync under os.tmpdir() and removes it after the run, keeping each scenario input next to its assertion. | FR-002, AC-006, and AC-007 mandate one fixture transcript under the fixtures tree carrying exactly one delegation-phrased event line and one invocation line. The remaining acceptance scenarios each need a distinct transcript, and folding them into the mandated fixture would contradict the acceptance scenario that fixes its contents. | accepted | docs/changes/mine-transcript-delegation-stem/design.yaml |
| 2026-08-28 | mine-transcript-delegation-stem (DEC-003) | The suite asserts AC-001 by spawning the miner with --verbose on the committed fixture and matching the three verbose sub-field labels; all count assertions use the default report. No miner output change is made or required. | AC-001 asserts the sub-fields type=subagent, model=resolved, and rework=none, which appear in the report only when the miner runs with --verbose; the default report carries only the delegations count row. | accepted | docs/changes/mine-transcript-delegation-stem/design.yaml |
| 2026-08-28 | mine-transcript-delegation-stem (DEC-004) | The design delta array stays empty and the delta step is marked reviewed; knowledge extraction applies the requirements-stage entry when the change lands. | The requirements stage already appended the Modify entry for the operations helper row, and the verified assumptions record that the skill documentation already describes the intended four-form grammar, so the fix aligns code with existing documentation. The design introduces no new capability, contract, or command beyond what the requirements delta records. | accepted | docs/changes/mine-transcript-delegation-stem/design.yaml |
| 2026-08-29 | envelope-state-flags (DEC-001) | Remove the flag entirely: the envelope stops exposing it because artifacts stop carrying it. The envelope top-level step field is the single source of truth for step-machine state. No engine migration logic strips the key from legacy files on write. | The metadata step flag is written at instantiation and finalization and never updated by the mutation path, so it drifts from the envelope top-level step field that the step detector recomputes on every call. Syncing it would preserve a drift-prone duplicate of state the envelope already carries, and a repository-wide search found zero in-repo consumers of the flag. | accepted | docs/changes/envelope-state-flags/design.yaml |
| 2026-08-29 | envelope-state-flags (DEC-002) | The data assembler derives semantic_complete from artifact metadata status: true exactly for ready-for-review and accepted, false for draft, rejected, and a missing artifact. The fabricated semantic summary is removed from the standard-output environment because the revalued derivation was its only live consumer. The review-readiness helper keeps reading the environment semantic summary unchanged; it is imported but never called today, and any future caller is expected to supply a real summary. | The envelope data assembler reads the environment semantic summary, which the standard-output path fabricates as complete. Real semantic checks surface only in the finalize confirmation flow, so the flag misreports an empty draft as complete. The tasks kind has its own implementation-status path and consumes neither the assembler nor the semantic summary, so it cannot regress. | accepted | docs/changes/envelope-state-flags/design.yaml |
| 2026-08-29 | envelope-state-flags (DEC-003) | The mirror stays verbatim and gains no key-specific logic. Correctness comes from three coordinated removals: no write site emits the key, the tightened schemas make the key a validation failure, and the one-time cleanup removes legacy keys. In a valid repository state the mirror can therefore never expose a step key. | The envelope mirrors artifact metadata verbatim into the data payload. Stripping the step key at exposure would add a per-key special case and would mask legacy files instead of fixing them. | accepted | docs/changes/envelope-state-flags/design.yaml |
| 2026-08-29 | envelope-state-flags (DEC-004) | Remove the optional step property from the metadata object in the three schema files. No other metadata property changes; the frozen envelope schema is untouched. | Only requirements, design, and planning carry their own schema.yaml with a step property in the metadata object. The implementation stage validates its plan artifact against the planning schema through its schema-from binding, so it is covered without its own file. The knowledge-extraction schema declares no step property and is untouched. Leaving the property would permit the exact stale-state field this change removes. | accepted | docs/changes/envelope-state-flags/design.yaml |
| 2026-08-29 | envelope-state-flags (DEC-005) | The cleanup deletes the stale step line from all artifact files under the changes directory as they exist at implementation time, including this change's own artifacts. The verification is a line search returning zero files, not a count comparison. If the sweep corrupts an artifact, that file is restored from git history before any review stage runs against it. | Artifact files authored before the engine fix lands carry the stale step line, and the count grows with every stage authored in the meantime; a frozen count would go stale before implementation. This change's own requirements file already carries the line, and its design and plan files will carry it because the current engine writes it. | accepted | docs/changes/envelope-state-flags/design.yaml |
| 2026-08-29 | envelope-state-flags (DEC-006) | Add test/unit/envelope-state-flags.test.ts covering the derivation across draft, ready-for-review, accepted, and rejected statuses, the absence of a step key after instantiation and finalization, and the envelope assembly for a representative stage. Extend the existing end-to-end authoring coverage with assertions that the envelope data payload reports the revalued flag and no metadata step key. | The data assembler, the mutation path, finalization, and the envelope assembly currently have no covering unit tests, so a regression in the new derivation would not be caught by the fast gate. End-to-end authoring tests exercise the real CLI but do not assert on the two flags today. | accepted | docs/changes/envelope-state-flags/design.yaml |
| 2026-08-29 | envelope-state-flags (DEC-007) | Implementation order within this change: engine change with unit tests first, then the corpus sweep, then schema tightening, then living-docs deltas, then the deploy smoke run that re-renders the bundle. The fast gate must pass at each task boundary; full coverage runs at the end. | Schema tightening makes any artifact carrying a step key fail validation, so tightening before the sweep would break finalization and review for legacy files, including this change's own artifacts. The deployed skill bundle embeds the stage schemas, so a schema change alters deploy output. | accepted | docs/changes/envelope-state-flags/design.yaml |
| 2026-09-02 | review-round-findings (DEC-001) | Define the known-id universe as all id-shaped strings matching ^[A-Z]{2,6}-[0-9]{2,4}$ collected from the change root's YAML artifacts (DM-004). A target containing a standalone id-shaped token must resolve into that universe; an unresolved id-shaped target emits the UNKNOWN_FINDING_TARGET warning naming the target, and the round is still recorded. A target with no id-shaped token is a free-text anchor: it skips resolution and never warns. This resolves FR-007/AC-013 against DL-008 — the warning fires exactly when a target claims to be an id and the claim fails. | FR-007 and AC-013 require a warning when an entry target matches no known artifact id, but DL-008 explicitly allows free-text section anchors as targets, and free text cannot resolve against an id universe without producing false warnings on every legitimate anchor. The accepted review asks to pin when the unknown-target warning fires. | accepted | docs/changes/review-round-findings/design.yaml |
| 2026-09-02 | review-round-findings (DEC-002) | The decision token set is exactly review, accepted, rejected, accept_blocked. When --accept runs with blocking mechanical findings, the round is completed closed with decision accept_blocked, no semantic block, and rationale composed from the mechanical findings; the artifact status is left unchanged and the envelope reports state blocked with CANNOT_ACCEPT. Planning should add explicit test coverage for this path since no AC exercises it (CMP-006 names it). | FR-003 and FR-009 imply that --accept with blocking mechanical findings still completes the round (the verdict ran), and the current interpreter already records the token accept_blocked in that case, but no AC exercises the path and the token appears nowhere in the requirements text — the accepted review asks the design to name it explicitly. | accepted | docs/changes/review-round-findings/design.yaml |
| 2026-09-02 | review-round-findings (DEC-003) | Follow FR-010: completeness is measured against the reviewed (target) stage's semantic-checks.yaml — exactly what semanticChecksFor(trackedStage) already reads — and review stages carry none. The CLI validates the shape, completeness, and status values of reviewer-supplied results (status must be 'pass' for every item on the accept path, FR-011) but never evaluates the checks themselves; they stay agent-walked. This acknowledges the tightened out-of-scope wording as a design boundary, not just a wording fix. | DL-009 says the semantic block covers every check in "the stage's semantic-checks.yaml" while FR-010 says "the target stage's semantic-checks.yaml"; review stages carry no semantic-checks.yaml of their own (per-kind file set), so DL-009's phrasing is ambiguous — the accepted review directs the design to follow FR-010. The requirements out-of-scope bullet was also tightened: validating reviewer-supplied results is in scope, evaluating check outcomes is not. | accepted | docs/changes/review-round-findings/design.yaml |
| 2026-09-02 | review-round-findings (DEC-004) | The round's rationale is the --note text when supplied; otherwise, when blocking mechanical findings exist, a deterministic summary of those findings; otherwise the field is omitted. The mechanical block always carries the full findings regardless of the precedence, so no information is lost by the choice. | FR-005 and DL-005 make the mechanical findings the rejection rationale when the reviewer supplies nothing, while FR-006 stores --note on the round; a supplied --note alongside blocking mechanical findings is not forbidden by any AC, so the design must fix one deterministic composition. | accepted | docs/changes/review-round-findings/design.yaml |
| 2026-09-02 | review-round-findings (DEC-005) | All input validation — flag conflicts, verdict-scoping, findings-file shape, semantic walk completeness — runs before the gate result is acted on and before any file is touched; every violation exits with a USAGE- or validation-coded error and neither the artifact nor the review file is written. The --findings path resolves relative to the process working directory, matching --record-answers behavior (assumption 5 — confirm during planning); round numbering increments only when a round is appended, never when an open round is refreshed (assumption 6 — confirm during planning). | AC-009 through AC-012 and AC-017 all demand that invalid input writes nothing, but the current interpreter writes the artifact status and the round deep inside the verdict branches, so validation must be hoisted ahead of every mutation. Two requirements assumptions also need planning confirmation: the --findings path resolution base (assumption 5) and round numbering on refresh (assumption 6). | accepted | docs/changes/review-round-findings/design.yaml |
| 2026-09-02 | the-sdlc-cli-resolves-the-project-s-docs-changes-directory (DEC-001) | Keep the flag as the sole override mechanism. This resolves the carry-forward without reopening requirements: with a flag-only mechanism, FR-005's "override flag" wording is exactly what ships, and DL-002's "and/or" explicitly delegated the narrower choice to design, so the requirements text needs no change and this decision records the resolution. An environment variable would add a second, envelope-invisible source of truth that is harder to test deterministically and can leak across agent sessions on long-lived runtimes. | DL-002 permits a CLI flag and/or an environment variable and leaves the mechanism to design; FR-005 and AC-009 speak of an override flag; --cwd already exists, is parsed by parseArgs, threaded through runner.ts and the status, feedback, and doctor workflows, and exercised by six existing e2e test files. The requirements review carried forward a wording tension between FR-005's "override flag" and DL-002's "flag and/or env var". | accepted | docs/changes/the-sdlc-cli-resolves-the-project-s-docs-changes-directory/design.yaml |
| 2026-09-02 | the-sdlc-cli-resolves-the-project-s-docs-changes-directory (DEC-002) | changesDirFor(cwd) lives in resolve-root.ts, the owner of the docs/changes contract, kept fs/path-pure for isolated unit testing; resolveCwd(args) lives in cli.ts, which every call site already imports, so no new module and no import cycle. The workflow dispatch closure adopts resolveCwd and thereby also removes its double parseArgs call. The registry lookups in resolveWorkflow and listWorkflows stay on process.cwd(): they resolve bundled engine assets, which the requirements place out of scope, their script-relative resolution shadows the cwd fallback in both source and deployed forms, and they never construct a docs/changes path, so AC-007's scope (direct construction of the docs/changes path from cwd) does not reach them. | FR-004 and AC-007 require one shared resolution function for docs/changes construction; the cwd-from-args pattern is separately duplicated six times; registry lookups in workflows/index.ts also call bare process.cwd(). | accepted | docs/changes/the-sdlc-cli-resolves-the-project-s-docs-changes-directory/design.yaml |
| 2026-09-02 | the-sdlc-cli-resolves-the-project-s-docs-changes-directory (DEC-003) | Replace the message with: docs/changes does not exist under <root> (searched: <root>/docs/changes). Run from the project root or pass the root explicitly with --cwd <project-root>. It preserves the asserted prefix so existing e2e passes unmodified, names both the resolved root and the full searched path (AC-005), states the two remedies, and drops "Create a change first" entirely (AC-006; the advice is also never necessary because --request auto-creates docs/changes via createChangeDir). data.searched remains the machine-readable carrier (NFR-002, AC-011). The single unit assertion expecting the old text is updated because AC-006 explicitly mandates that text change, while every resolution expectation in the file (searched path, available list, no-walk-up) stays untouched, so AC-010's resolution-expectation clause holds. If an external consumer matching the old text is ever found, the requirements failure_path prescribes keeping the old message as a secondary detail line; no such consumer is known and the residual risk is accepted as low per the requirements. | The misleading text lives only in resolveRootOrError's not-exists branch; every resolution-failure surface renders ResolveRootError.message, so one fix makes all surfaces consistent. The e2e suite asserts the message prefix "docs/changes does not exist under <path>", and one unit assertion expects the old "Create a change first." text. | accepted | docs/changes/the-sdlc-cli-resolves-the-project-s-docs-changes-directory/design.yaml |
| 2026-09-02 | the-sdlc-cli-resolves-the-project-s-docs-changes-directory (DEC-004) | Add test/unit/cwd-resolution.test.ts for the five-scenario function matrix; extend test/e2e/change-resolution.test.ts with a --cwd override success case and an invalid---cwd blocked-envelope case, spawn-based to match the file's existing pattern; update exactly one message-text assertion in test/unit/resolve-root.test.ts (the AC-006-mandated change) and zero resolution expectations. New envelopes are asserted against the seven frozen top-level fields to keep AC-011 under surveillance. | The resolution matrix (project root, non-root directory, override, invalid override, no-walk-up) is function behavior of resolveRootOrError and changesDirFor; flag threading into runner, status, feedback, and doctor is CLI behavior already partially exercised by six e2e files that pass --cwd. | accepted | docs/changes/the-sdlc-cli-resolves-the-project-s-docs-changes-directory/design.yaml |
| 2026-09-02 | the-sdlc-cli-resolves-the-project-s-docs-changes-directory (DEC-005) | Sequence (1) engine change plus unit tests, (2) npm run validate, (3) npm run test:e2e, (4) npm run deploy:smoke as the final shipping step so the bundle re-render is part of the change rather than an afterthought; the plan must sequence deploy last. Design-time verification found that review, tasks, and aggregator kinds already receive cwd via runStage, so the stop-condition does not trigger and no signature change is needed. | The deployed bundle embeds the compiled CLI, so a source-only change leaves the deployed skill with the old behavior (requirements risk); the requirements failure_path prescribes stopping if a kind interpreter cannot receive the resolved root without an unplanned signature change. | accepted | docs/changes/the-sdlc-cli-resolves-the-project-s-docs-changes-directory/design.yaml |
| 2026-09-03 | ac-parent-backref-check (DEC-001) | Put the grammar and both resolution functions in a new src/scripts/lib/artifact-paths.ts. checks/shared.ts delegates getTopArray and resolvePath to it and keeps its exported signatures; ids.ts imports the same module. The extension is purely additive: single-segment specs produce byte-identical targets and findings. | Multi-segment resolution is required by four checks and by next-id allocation in ids.ts. Extending resolvePath inside src/scripts/lib/checks/shared.ts in place would force ids.ts, a generic utility, to import from the checks folder, inverting the layering; keeping two implementations would drift. | accepted | docs/changes/ac-parent-backref-check/design.yaml |
| 2026-09-03 | ac-parent-backref-check (DEC-002) | unique-ids keeps arrays (each entry one scope) and gains unions (each group one scope across the listed paths). given-when-then renames its parameter to arrays and accepts a string or a list whose members resolve to one union; it is declared only by the requirements stage, which is rewritten in this change. ref-exists resolves path entries in to.arrays, where the valid-id set already unions. from.array stays a top-level selector; nested from-specs are out of scope. | AC ids must be unique across the union of FR-nested and NFR-nested criteria, while existing plain-name arrays (planning milestones and risks, requirements FR/NFR/DL) must keep per-array uniqueness with byte-identical findings. Implicitly unioning every path entry would blur the two semantics and risk altering existing behavior. | accepted | docs/changes/ac-parent-backref-check/design.yaml |
| 2026-09-03 | ac-parent-backref-check (DEC-003) | Validate [].-bearing parameter strings against the stage schema (and the target stage schema for ref-exists to.file, resolved through the registry, with a grammar-only fallback for files no stage owns) inside validateArtifact before runStageChecks and inside bin/validate-policies.ts at startup. Abort with a thrown Error naming the stage folder and the declaration. No new errors.yaml codes are needed; the thrown-Error pattern is reused. | An artifact instance cannot distinguish an unknown array named by a declaration from an artifact that legitimately lacks the arrays, and an artifact-based abort would swallow schema findings for old-format artifacts. The existing unknown-check abort is a thrown Error naming the stage folder and the entry; errors.yaml codes serve CLI envelopes, not the check layer. | accepted | docs/changes/ac-parent-backref-check/design.yaml |
| 2026-09-03 | ac-parent-backref-check (DEC-004) | Widen next_ids values to string or string list; list entries resolve as paths through the shared resolver and the collected ids union before nextId. stage.yaml remains the single declaration point; the registry typing and the stage meta-schema widen to match. String specs behave exactly as today. | AC allocation must scan criteria nested under both FR and NFR entries. nextIdsFromArrays is shape-aware today through the AC spec reading the top-level array; hard-coding the nesting inside ids.ts would make the allocator stage-aware and duplicate the path logic. | accepted | docs/changes/ac-parent-backref-check/design.yaml |
| 2026-09-03 | ac-parent-backref-check (DEC-005) | Choose (a): declare the retired fields explicitly with boolean-false schemas in the requirements schema properties — acceptance_criteria: false at the root, ac_ids: false on FR and NFR entries, parent_id: false on criterion items — in addition to additionalProperties false. The rendered findings name the retired field in the instance path with no engine change, keeping the change contained in the stage folder; pinned against the repository's Ajv 8.20.0 with the validate.ts rendering rule: '/acceptance_criteria boolean schema is false' (top-level acceptance_criteria), '/functional_requirements/0/ac_ids boolean schema is false' (entry ac_ids), and '/functional_requirements/0/acceptance_criteria/0/parent_id boolean schema is false' (criterion parent_id through the definitions $ref). | FR-002, AC-003, AC-004, and requirements failure_paths[0] demand schema findings that name the retired field. The first design round specified additionalProperties false as the rejection mechanism; design-review round 2 falsified this empirically with the repository's own Ajv (allErrors: true, the proposed schema shape including definitions/$ref): validate.ts renders findings as instancePath plus message, Ajv's additionalProperties message is the static 'must NOT have additional properties', and the property name appears only in err.params.additionalProperty, which validate.ts never renders — no finding names the retired field. Two schema-layer-compatible options were verified: (a) boolean-false property schemas for the retired fields, contained entirely in the stage folder with no engine change; (b) a validate.ts rendering change appending err.params.additionalProperty to the finding text, which touches the shared engine and alters every schema finding's shape, requiring re-pinning of schema-finding tests across all stages. | accepted | docs/changes/ac-parent-backref-check/design.yaml |
| 2026-09-03 | ac-parent-backref-check (DEC-006) | No migration, no dual-format support, no dedicated retired-format error. Git history preserves old artifacts; hand re-validation of an archived artifact fails schema validation by design and is out of contract. | Fifteen accepted old-format changes exist. The acceptance gate reads artifact status, not schema, and archived changes are never re-validated by any pipeline flow. | accepted | docs/changes/ac-parent-backref-check/design.yaml |
| 2026-09-03 | ac-parent-backref-check (DEC-007) | All stage-folder, schema, and check-library changes land under src/ and bin/. npm run deploy:smoke regenerates the bundle; no manual edits under .opencode are part of this change. | The bundled stages, schemas, and policies under .opencode are build artifacts regenerated by deployment, and repository rules forbid manual edits there. | accepted | docs/changes/ac-parent-backref-check/design.yaml |
| 2026-09-03 | ac-parent-backref-check (DEC-008) | Migrate validRequirements first, then the five direct builders; helper-only consumers inherit without edits. Add pinning tests that record the exact findings of the unchanged design and planning declarations against their existing fixtures and assert them byte-identically after the change; add new unit coverage for the resolver, union uniqueness, nested finding targets, the malformed-path abort, the ten-check catalog, and nested allocation. | Six files build flat-format fixtures (the shared builder plus five unit files) and seven e2e files consume the shared builder. The change must not alter findings for existing single-segment declarations in design and planning. | accepted | docs/changes/ac-parent-backref-check/design.yaml |
| 2026-09-03 | ac-parent-backref-check (DEC-009) | template.yaml drops the top-level acceptance_criteria key and seeds functional_requirements with one scaffold entry carrying id FR-001, a replace-me description, and a nested acceptance_criteria array holding one example criterion (id AC-001, a Given-When-Then statement, category happy), so the parsed scaffold itself shows the nested shape. steps.yaml instructs the authoring agent to replace the scaffold entry with real requirements and criteria. bin/validate-templates.ts asserts the seeded example, pinning AC-019 at the fast gate. | Requirements AC-019 requires the scaffold to show acceptance_criteria nested inside each requirement entry. The scaffold is the parsed template: instantiateArtifact deep-clones the parsed template.yaml and substitutes only metadata fields, so guidance text never enters it and YAML comments do not survive init. The first design round delegated the demonstration to steps.yaml guidance and was rejected on this finding; the stakeholder resolved the earlier residual question by requiring the literal example despite seeding example content into fresh artifacts. | accepted | docs/changes/ac-parent-backref-check/design.yaml |
| 2026-09-03 | remove-based-on-provenance-fields-and-gate-artifact-creation (DEC-001) | Remove based_on_requirements and based_on_design from every stage schema, template, hook envelope exposure, instantiation output, helper, and test (FR-001). The requires graph plus live ref-exists validation is the sole traceability mechanism. This decision supersedes the DEC-010 provenance-only rationale recorded in validate.ts. | DEC-010 kept the fields as provenance-only because validation deliberately ignored their values. Discovery (DL-001) confirmed there is no machine consumer: traceability is enforced live by ref-exists against the current predecessor artifacts, exactly one active version of each artifact exists per change, and the recorded version strings have no enforced meaning. | accepted | docs/changes/remove-based-on-provenance-fields-and-gate-artifact-creation/design.yaml |
| 2026-09-03 | remove-based-on-provenance-fields-and-gate-artifact-creation (DEC-002) | Evaluate the acceptance gate on every path that first-creates a non-root authoring artifact — inside createChangeDir before any write, in the inline lazy-instantiation block of runAuthoringStage, and in ensureArtifact — by calling evaluateGate unchanged (FR-002). The blocked envelope reuses STAGE_GATE_BLOCKED with data.unsatisfied_requirements, identical to the finalize-time gate. Stages with an empty requires list skip the gate, and finalize-time gate semantics are untouched. | Artifact creation is currently ungated; the only early signal is an init step predicate over the very fields being removed. The finalize-time gate already implements the acceptance rule over the requires graph, and duplicating that rule would create a second dependency semantics. | accepted | docs/changes/remove-based-on-provenance-fields-and-gate-artifact-creation/design.yaml |
| 2026-09-03 | remove-based-on-provenance-fields-and-gate-artifact-creation (DEC-003) | The creation gate derives everything from the stage descriptor requires list and the tracked-artifact resolution of the requires graph; no engine code special-cases a stage id or artifact filename, and no based_on field is added to stage.yaml. The decisive re-rooting criterion — deleting requires from src/stages/design/stage.yaml makes design a valid starting stage with zero code change — is a stated design property and a planned unit test over a synthetic stages directory (AC-008, AC-016). | FR-003 and NFR-001 require that the creation decision derive solely from descriptor data so any pipeline reordering expressed through requires lists enforces identically at creation and finalize with zero engine change, and out_of_scope forbids introducing a based_on descriptor field. | accepted | docs/changes/remove-based-on-provenance-fields-and-gate-artifact-creation/design.yaml |
| 2026-09-03 | remove-based-on-provenance-fields-and-gate-artifact-creation (DEC-004) | Migrate with a one-time line-level filter that removes exactly the metadata lines for based_on_requirements and based_on_design from every docs/changes/*/design.yaml and plan.yaml, preserving every other byte; no version bump; review files untouched (FR-005). AC-012 is verified by running bin/lint-artifact.ts over every migrated artifact against the updated schemas; AC-013 is verified by a git diff audit showing the only differences are the deleted key lines with version, status, created, and review files unchanged. | Stage metadata schemas declare additionalProperties false, so existing artifacts carrying the removed keys would fail validation after the schema update (DL-003). A YAML round-trip would reformat the files and violate the only-difference criterion AC-013. | accepted | docs/changes/remove-based-on-provenance-fields-and-gate-artifact-creation/design.yaml |
| 2026-09-03 | ac-parent-backref-check (requirements) | Each FR and NFR entry carries its own nested acceptance_criteria array (id AC-NNN, Given-When-Then statement, category happy/edge/negative/boundary), replacing the flat merged top-level criteria list; ac_ids and parent_id are retired and referenced-by is removed from the check catalog, leaving ten named checks — superseding the flat merged list of streamline-the-sdlc-process-economy-collapse-the-authoring-s (DEC-001) | Criteria are authored, validated, and reviewed inside their owning requirement, eliminating the cross-reference fields (ac_ids, parent_id) whose only job was stitching a flat list back to its parents; the referenced-by check existed solely to police those fields | accepted | docs/changes/ac-parent-backref-check/requirements.yaml, design.yaml |
| 2026-09-07 | agents-working-sdlc-stages-are-too-verbose-in-chat-replies (DEC-001) | AgentRecord gains effectivePrompt composed once at registry load (role prompt, blank line, fragment); renderers emit it verbatim; source YAML is never mutated. Any future platform renderer inherits the fragment for free by reading the composed record. | The fragment must reach every rendered agent across both renderer formats with no per-agent registration and no renderer duplication - the effectiveModel pattern already established registry-level composition of effective values (agent-layer DEC-004). | accepted | docs/changes/agents-working-sdlc-stages-are-too-verbose-in-chat-replies/design.yaml |
| 2026-09-07 | agents-working-sdlc-stages-are-too-verbose-in-chat-replies (DEC-002) | The fragment lives as an exported constant in a dedicated engine module (src/scripts/lib/agent-output-discipline.ts) beside its compose and detection helpers - the agent-prompt-marker.ts pattern. No new config surface, no schema change, no build step. | FR-002 demands one source of truth rendered into every agent - a YAML include mechanism is out of scope, and the text must be importable by the registry, renderers, smoke test, and startup validation alike. | accepted | docs/changes/agents-working-sdlc-stages-are-too-verbose-in-chat-replies/design.yaml |
| 2026-09-07 | agents-working-sdlc-stages-are-too-verbose-in-chat-replies (DEC-003) | The fragment module exports distinctive marker phrases derived from its own text - validate-policies fails any descriptor prompt containing one, naming the file and marker (new AGENT_FRAGMENT_COPY code in src/policies/errors.yaml). Markers live beside the text so they cannot drift from it - rewording the fragment updates its markers deliberately. | AC-006 requires rejecting agent descriptors whose system_prompt embeds the fragment - exact full-text matching is brittle (whitespace drift breaks it) and would miss near-copies; detection must be deterministic and unit-testable. | accepted | docs/changes/agents-working-sdlc-stages-are-too-verbose-in-chat-replies/design.yaml |
| 2026-09-07 | agents-working-sdlc-stages-are-too-verbose-in-chat-replies (DEC-004) | The meta-schema stays structurally unchanged - its system_prompt description documents the role-voice-only contract, and enforcement lives in the validate-policies agent-layer check beside prompt purity. The capped structural-check catalog is untouched - agent-descriptor validation is engine-level config validation, not a stage-artifact check. | The requirements' touch list names the agent meta-schema, but a schema-level ban would have to duplicate fragment text into a draft-07 regex (fragile, and it violates the single-source rule), while per-agent fragment fields are explicitly out of scope. | accepted | docs/changes/agents-working-sdlc-stages-are-too-verbose-in-chat-replies/design.yaml |
| 2026-09-07 | agents-working-sdlc-stages-are-too-verbose-in-chat-replies (DEC-005) | The canonical failures-only rule is fixed once in this design (DM-003) and carried verbatim in the four review steps.yaml files and the stage-reviewer prompt - alignment is verified at design-review, not by a structural check. | AC-011 is inspection-based (the step markdown is inspected) - prose wording is not a structural concern, and adding a capped-catalog check for it would be a design-review event with no mechanical payoff - a YAML include mechanism is out of scope, so the four step files must repeat the rule. | accepted | docs/changes/agents-working-sdlc-stages-are-too-verbose-in-chat-replies/design.yaml |
| 2026-09-07 | agents-working-sdlc-stages-are-too-verbose-in-chat-replies (DEC-006) | Only the guidance text in GUARDRAILS and the implementation steps changes, to the one-sentence what-changed-plus-how-verified rule - the write-time guard and the structural check keep their exact behavior (AC-015). | The note guard (TASK_DONE_REQUIRES_NOTE) and required-note-for-status already enforce note presence - verbosity came from guidance that invited narration, and the primary consumers of notes are AI agents. | accepted | docs/changes/agents-working-sdlc-stages-are-too-verbose-in-chat-replies/design.yaml |
| 2026-09-07 | output-discipline-dissolved-into-per-agent-prompts | The shared output-discipline fragment module and its deploy-time composition were removed; each agent carries its discipline text inline in its own system_prompt | Per-agent flexibility — each agent owns its instructions | accepted | src/agents/, src/scripts/lib/agent-registry.ts, src/scripts/lib/deploy/platforms/opencode.ts |
| 2026-09-09 | cli-surface-and-permissions (DEC-001) | All content flags accept <file\|-> where bare flag or '-' means stdin and a path means file; docs steer simple cases to stdin; payloads live under .tmp/sdlc with the prescribed unique-name pattern; deletion is the caller's job. | Two input styles existed (stdin-only flags vs file-or-stdin flags); agents need a retry loop that survives failed submissions, and temp files need a writable location under the tightened permissions. | accepted | docs/changes/cli-surface-and-permissions/design.yaml |
| 2026-09-09 | cli-surface-and-permissions (DEC-002) | The Read tool remains the read surface; the CLI stays write-oriented. | Only writes are denied for docs/changes; reads through the Read tool are unaffected, so a show command would add envelope surface without removing an obstacle. | accepted | docs/changes/cli-surface-and-permissions/design.yaml |
| 2026-09-09 | cli-surface-and-permissions (DEC-003) | Help and docs advertise upsert semantics on --update-artifact; lazy instantiation stays the fallback. | ensureArtifact already instantiates (gated) before merging, so creation-with-content is one call; an explicit verb would add surface without capability. | accepted | docs/changes/cli-surface-and-permissions/design.yaml |
| 2026-09-09 | cli-surface-and-permissions (DEC-004) | Uniform upsert semantics apply to the authoring family only; tasks gains the sanctioned narrow task flags; review keeps verdict verbs; the aggregator stays CLI-computed. | Generic artifact edits on review, tasks, and aggregator kinds would bypass round governance and the planning review. | accepted | docs/changes/cli-surface-and-permissions/design.yaml |
| 2026-09-09 | cli-surface-and-permissions (DEC-005) | Remove all four surfaces; versioning follows the mechanical rules (none, patch, minor), mutations always apply the strict draft-reset, next_ids ride the authoring-step envelope. | --bump-version overrode rules the lifecycle already determines and nothing consumes the override; --keep-status bypassed the review guarantee; --next-ids duplicated step-envelope data; the review --target command and tombstones are retired scaffolding. | accepted | docs/changes/cli-surface-and-permissions/design.yaml |
| 2026-09-09 | cli-surface-and-permissions (DEC-006) | Each command declares its flag table once; the help contract renders it and the guard enforces it; completeness tests bind the two. | Generic argument parsing silently ignored typo'd flags, and a typo'd mutation flag degrades into a bare invocation. | accepted | docs/changes/cli-surface-and-permissions/design.yaml |
| 2026-09-09 | cli-surface-and-permissions (DEC-007) | Delta entries carry stage (lowercase id, per-stage enum); the aggregator stops annotating; dedupe sorts by stage; a one-time sweep renames baked keys. | Every phase value equaled the capitalized stage id and the aggregator already annotated source_stage, making the field derivable twice over. | accepted | docs/changes/cli-surface-and-permissions/design.yaml |
| 2026-09-09 | cli-surface-and-permissions (DEC-008) | Change-less workflow invocations are usage errors; the generated SKILL.md owns the four-shape identification protocol; the authoring tour becomes authoring, ready, complete, recovery; context loading is an AGENTS.md duty, not a step. | A change-less stage invocation cannot know which change is meant, and the needs_input and init steps duplicated AGENTS.md always-in-force duties. | accepted | docs/changes/cli-surface-and-permissions/design.yaml |
| 2026-09-09 | cli-surface-and-permissions (DEC-009) | Scope grants per kind through the existing protected-path pattern machinery with base deny first and scratch allows after, tighten the engine contracts to deny-ceilings, and gate the result with the deploy smoke. | Authoring and aggregator agents held allow-everywhere write floors while their only durable targets are docs/current and CLI-mediated artifacts. | accepted | docs/changes/cli-surface-and-permissions/design.yaml |
| 2026-09-09 | cli-surface-and-permissions (DEC-010) | Extend mergeArtifact in place for id-keyed merging and extend scopeProtectedPaths for the new grants; no new merge or permission modules. | Both behaviors extend mechanics that already exist and are covered by tests. | accepted | docs/changes/cli-surface-and-permissions/design.yaml |
| 2026-09-09 | retire-docs-index-single-routing-map (DEC-001) | Remove the index whole-file entry from docs-gen.yaml, delete docs/current/index.md and its provider, and extract the seven cross-checks into the non-rendering module src/scripts/lib/docs-gen/crosscheck.ts invoked by generateAll so docs:generate and docs:check still throw on violations; nothing about check results is persisted anywhere. This retires and supersedes the DEC-003/DEC-004 index-table contract: enforcement-only keeps the gate while removing the drift surface, and AGENTS.md section 4 — auto-loaded by the runtime — is the single map, so no successor routing or index artifact is created in docs/current. | docs/current/index.md duplicated the AGENTS.md section 4 routing table and persisted the seven mechanical cross-checks and the dead-reference scan as status tables. The persisted tables descend from DEC-003/DEC-004 of the deploy-knowledge-init change (index.md as the single entry document with a contract-frozen document table); an 'all clear' table is historical evidence, not enforcement — the same run's violations were rendered as rows instead of failing the gate, which let two stale references survive in the central error policy. | accepted | docs/changes/retire-docs-index-single-routing-map/design.yaml |
| 2026-09-09 | retire-docs-index-single-routing-map (DEC-002) | Promote the scan to a hard failing check inside the same crosscheck gate, with no rendering anywhere (index provider deleted; the known-issues provider stops consuming scanDeadReferences so known-issues.md carries no STALE-REF rows), and fix the two known stale references so the check passes on a clean tree (DEC-003). A stale reference now fails npm run validate instead of being documented. | scanDeadReferences (src/scripts/lib/docs-gen/scans.ts) is render-only today: the index provider rendered its hits as a Notes table and the known-issues provider rendered them as STALE-REF marker rows, so a stale reference never failed a run — the failure mode that let two stale paths survive in src/policies/errors.yaml. | accepted | docs/changes/retire-docs-index-single-routing-map/design.yaml |
| 2026-09-09 | retire-docs-index-single-routing-map (DEC-003) | MANIFEST_INVALID is repointed to the real path bin/deploy-to-agent.ts. ILLEGAL_STATUS_TRANSITION drops the stale path token: the fix text names the artifact status vocabulary inline (draft, ready-for-review, accepted, rejected, blocked) instead of citing a file — repointing to any single stage schema would be arbitrary because every stage declares its own status enum, and an inline vocabulary keeps the catalog-only entry actionable. Both corrections are verified by the hard STALE-REF check (FR-004). | After reading src/policies/errors.yaml: the MANIFEST_INVALID fix cites bin/deploy-to-agent.mjs while the real deploy script is bin/deploy-to-agent.ts; the ILLEGAL_STATUS_TRANSITION fix cites src/policies/lifecycle.yaml, which does not exist — there is no lifecycle policy file, artifact status vocabularies are declared per stage in the stage schemas, and the codebase has no ILLEGAL_STATUS_TRANSITION emitter (catalog entry only, rendered into the api-contract error-catalog region). | accepted | docs/changes/retire-docs-index-single-routing-map/design.yaml |
| 2026-09-09 | retire-docs-index-single-routing-map (DEC-004) | loadDocsIndex(cwd) returns sorted {file} entries ('docs/current/<name>.md') read from the directory; parseDocsIndex and the purpose/when/notes fields are removed with no remaining importers; headingExists and normalizeHeading stay because Modify/Remove delta entries still validate target_anchor against the target document. The authoring error becomes 'target_doc is not a docs/current document' and the warning becomes DOCS_CURRENT_MISSING with the reworded message. | loadDocsIndex parses the rendered index table into {file, purpose, when, notes} entries, coupling delta-target validation to a rendered artifact's column layout (DL-003). FR-006 replaces the registry with a sorted readdir of docs/current/*.md. | accepted | docs/changes/retire-docs-index-single-routing-map/design.yaml |
| 2026-09-09 | retire-docs-index-single-routing-map (DEC-005) | Drop the 'Start every session from docs/current/index.md' sentence; state that the table is the authoritative map of docs/current — a selection menu keyed by session goal — and add exactly two rows inside the existing section 4 table: docs/changes/<slug>/ (change artifacts — requirements/design/plan/review; in progress or accepted, no separate archive) and docs/ideas/ (deferred corrections noticed mid-session, picked up in a later session). The table grows to 13 of 16 lines and the file to roughly 51 of 90 — caps hold with headroom, no new section, no cap change. | With index.md retired, agents need one always-loaded selection menu keyed by session goal (DL-002, DL-007, DL-010). The file sits under validate:agents-md caps of 90 total lines and 16 table lines (currently 50 lines, 11 table lines), and the section allowlist forbids new sections. | accepted | docs/changes/retire-docs-index-single-routing-map/design.yaml |
| 2026-09-09 | retire-docs-index-single-routing-map (DEC-006) | Add the reverse direction inside the existing lintAgentsMarkdown policy (executed by bin/validate-agents-md.ts via npm run validate:agents-md): every docs/current/<name>.md mention in AGENTS.md — table cells and backticks — must exist in docs/current; the check's scope is exactly docs/current/<name>.md mentions, so docs/changes/<slug>/ and docs/ideas/ references are never flagged by it. This check extension is a design-review event with unit tests in test/unit/agents-md-policy.test.ts (invariants 4 and 5). | FR-009/DL-008 require AGENTS.md-to-docs/current reference validation; invariant 4 forbids stage-specific validation scripts and names existing policies as the extension path. The forward direction (docs/current to AGENTS.md routing-completeness) already lives in lintAgentsMarkdown and reads the directory, so it auto-adapts when index.md disappears. | accepted | docs/changes/retire-docs-index-single-routing-map/design.yaml |
| 2026-09-09 | retire-docs-index-single-routing-map (DEC-007) | The cross-check logic moves verbatim from the index provider into crosscheck.ts (behavior preservation, AC-006), reusing findImportSites/scanDeadReferences and the existing table parsers; the reverse-direction policy check adapts the existing PolicyContext, whose docsCurrentFileNames is already directory-derived; the knowledge-init cross-check section defers to the mechanical enforcement and emits nothing rather than duplicating the checks in prose. No engine rewrite, no new validation script, no duplicated check logic. | The seven cross-checks, the scan helpers (scans.ts), the table parsers (table.ts), and the policy lint (agents-md-policy.ts) already exist and are the reviewed, tested implementations of the behavior this change must preserve; the semantic review requires reuse to be evaluated and recorded. | accepted | docs/changes/retire-docs-index-single-routing-map/design.yaml |
| 2026-09-10 | orchestrator-owned-init-and-skill-hardening (DEC-001) | Add pipeline_state (in_progress \| complete) to each data.changes[] entry and keep the single list; the skill filters for selectability in its identification text (FR-021) and presents completed entries as archived. | FR-005 leaves the shape open — a per-entry field or separated selectable/archived lists. The changes envelope already carries one homogeneous data.changes[] list consumed by the skill identification flow, the help-contract tests, and the glossary row; splitting would introduce a second collection, a second shape to keep in sync, and a bigger envelope diff. | accepted | docs/changes/orchestrator-owned-init-and-skill-hardening/design.yaml |
| 2026-09-10 | orchestrator-owned-init-and-skill-hardening (DEC-002) | Verified — review.ts already writes data.round (the recorded round number) and data.status on every recorded review envelope, and round numbers increment exactly when a verdict closes a round, so data.round at a rejection equals the authoring run count. No CLI change; the skill reads the guard from the envelope it already receives. | FR-008 needs a CLI-visible authoring run count at review-round rejections; the requirements flagged as unverified whether the review envelope exposes one. | accepted | docs/changes/orchestrator-owned-init-and-skill-hardening/design.yaml |
| 2026-09-10 | orchestrator-owned-init-and-skill-hardening (DEC-003) | Keep it. The marker list guards agent system prompts against CLI machinery vocabulary, not against the current flag set — a prompt mentioning a removed flag is still machinery leak and the marker catches stale prompts at zero cost. No validate-policies change. | With --request removed from the CLI, the '--request' entry in PROMPT_MARKERS (agent-prompt-marker.ts) could be seen as dead vocabulary; the alternative is deleting it. | accepted | docs/changes/orchestrator-owned-init-and-skill-hardening/design.yaml |
| 2026-09-10 | orchestrator-owned-init-and-skill-hardening (DEC-004) | Place the gate at bin/validate-skill-refs.ts (bin/validate-agents-md.ts pattern — standalone bin script, JSON findings, non-zero exit) wired as validate:skill-refs into the npm run validate chain after validate:templates. Extraction is scoped to the sdlc CLI surface and regex-based — an sdlc invocation context is a token run whose command token is bare sdlc or a path ending in /sdlc.js or /sdlc.ts, extending to the end of the enclosing line or code span, read as an invocation only when the token after the command token is a command-shaped id or a flag (prose runs like sdlc ... and <stage> templates are skipped). Within a context the command id is checked against listCommands() ids plus the aliases map and every --<flag> token against the union of the help.ts flag tables plus the top-level --list-commands, --help, --cwd; bare --<flag> tokens outside sdlc invocation contexts are out of scope by construction — no exemption list. This scoping is what keeps the gate green on arrival — src/skills/improvement-review/SKILL.md lines 112, 117, 120, and 137 carry --verbose for its own bundled helper scripts (measure_artifacts.ts, envelope_sizes.ts, validate_duration.ts, mine_transcript.ts), which are not sdlc invocations and whose flags no help.ts table contains; FR-017's every command and flag reference reads as every reference to the sdlc CLI surface, which the invocation-context scope covers exactly. Envelope-field assertions are unit tests in test/unit/skill-envelope-refs.test.ts (state vocabulary round-tripped through normalizeEnvelope; data.stage and data.semantic_checks asserted against status.ts / review.ts emitting code). Rejected — markdown AST parsing (overkill for token references), bare --<flag> extraction over the whole skill body (red on arrival per the helper-script flags above), a per-file exemption list for improvement-review (hides real drift behind a carve-out), and extending the stage-check catalog (a repo gate is not a stage check). | FR-017 requires a repo-level gate but leaves placement and extraction open. SKILL.md bodies are prose, so extraction must be deterministic and cheap, and the reference universe must come from the same modules the CLI uses so the gate cannot itself drift. | accepted | docs/changes/orchestrator-owned-init-and-skill-hardening/design.yaml |
| 2026-09-10 | orchestrator-owned-init-and-skill-hardening (DEC-005) | New src/scripts/commands/init.ts reuses validateChangeSlug and the ChangeSlugError codes and messages verbatim (identical INVALID_CHANGE_SLUG and CHANGE_DIR_EXISTS behavior), but does NOT reuse createChangeDir — that function seeds an artifact and evaluates the creation gate, both forbidden for init; init is mkdir-only. Registered in commands/index.ts as a cross-cutting command (agent null) with a minimal INIT_FLAGS table (change required), so the closed-vocabulary guard yields UNKNOWN_FLAG for --request by construction (AC-004). createChangeDir itself is removed with the branches (CMP-002) — dead once its two call sites (authoring.ts 879, 948) are deleted, and init does not reuse it; the caller check then confirms slugify/uniqueSlug orphaned (their sole caller was inside createChangeDir) and both exports are removed, while validateChangeSlug stays for init. | The init contract (DL-003) overlaps existing machinery — validateChangeSlug, the ChangeSlugError codes, and createChangeDir all exist; reuse or adaptation had to be evaluated. | accepted | docs/changes/orchestrator-owned-init-and-skill-hardening/design.yaml |
| 2026-09-10 | orchestrator-owned-init-and-skill-hardening (DEC-006) | The rewrite preserves 'delegate the stage to that agent', 'only via the sdlc CLI', and the identification-rule phrase verbatim in the new text; DELEGATION_RULE_MARKER and the other marker constants stay unchanged. Updating a marker constant is explicitly rejected — the phrases are still the intended wording. | The SKILL.md rewrite rewords the delegation and artifact rules, but deploy-smoke gates on three marker phrases; losing one fails every deploy (FP-013). | accepted | docs/changes/orchestrator-owned-init-and-skill-hardening/design.yaml |
| 2026-09-10 | orchestrator-owned-init-and-skill-hardening (DEC-007) | The kickoff section prescribes sdlc init --change <judged-slug> followed by sdlc requirements --change <judged-slug>, and states init is change management rather than a stage — the acting skill agent runs it after the slug is judged and confirmed, then engages requirements. A slug colliding with an existing change adjudicates as resume-or-supersede, not a suffixed creation. | The improvement-review skill's kickoff command uses requirements --request, which the removal deletes; the idea-to-change flow needs a replacement that respects the orchestrator/stage boundary (DL-005). | accepted | docs/changes/orchestrator-owned-init-and-skill-hardening/design.yaml |
| 2026-09-10 | orchestrator-owned-init-and-skill-hardening (DEC-008) | Record the relationship only — this change's delegation template and loop guard cover the loop-closure intent, and orchestration-economy is to be superseded or adjusted in a later session; no edit to that change's artifacts in this run. | The orchestration-economy change (draft, requirements stage, 2026-09-07) carries loop-closure intent that this change's SKILL.md rewrite subsumes; acting on that change is out of scope here. | accepted | docs/changes/orchestrator-owned-init-and-skill-hardening/design.yaml |
| 2026-09-10 | orchestrator-owned-init-and-skill-hardening (DEC-009) | The path example is the CLI resolution sentence with a concrete invocation — resolve scripts/sdlc.js relative to the skill base directory and run node <skill-base>/scripts/sdlc.js status --change <name> from the project root, never cd into the skill folder. The worked example is a compact fenced block of one full cycle — changes inventory, init, heartbeat, delegate, heartbeat — with one-line comments tying each line to its loop step, six to eight lines total. | FR-022 requires one concrete path example and one 6-8 line worked example; the exact reading was left to design. | accepted | docs/changes/orchestrator-owned-init-and-skill-hardening/design.yaml |
| 2026-09-10 | cli-terminology-contract (DEC-017) | The envelope identity field is renamed to command and the status/changes data key naming the stage to run becomes stage (one term, one meaning — AGENTS.md §2.9); the registry listing flag becomes --list-commands with data key data.commands[], with no compatibility aliases. The delegation-directive machinery is removed from all envelopes, superseding the CMP-002 prepend placement in normalizeEnvelope: envelopes carry facts and actionable next steps only, and delegation rules live solely in the deployed skill, enforced by the deploy-smoke marker phrase. | The word "workflow" named two concepts (the envelope identity field and the pipeline stage to run), and per-envelope delegation prepends duplicated instructions the orchestrator skill already owns while inflating every envelope; the slim status/changes shapes keep the loop actionable through stage, agent, and suggested_command. | accepted | src/scripts/lib/cli.ts, src/schemas/cli-envelope.schema.yaml, bin/deploy-to-agent.ts, src/skills/agentic-sdlc/SKILL.md |
